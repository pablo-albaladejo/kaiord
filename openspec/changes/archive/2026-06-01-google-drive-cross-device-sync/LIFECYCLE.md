# Lifecycle Dashboard — google-drive-cross-device-sync

> Generated by openspec-lifecycle workflow

Artifacts complete: **yes**. Phase 1 (local snapshot export/import + tombstones, no OAuth), Phase 2 (CloudSyncPort + Google Drive adapter), and Phase 3 (syncWithCloud merge engine) are all **complete and fully green**. OAuth is provisioned and the `drive.appdata` scope is confirmed non-sensitive, so no Google verification gate exists for development. Phases 4–6 remain gated only on their upstream code dependencies (sequential implementation order).

## Phase progress

| Phase                                                    | Tasks done/total | Status   |
| -------------------------------------------------------- | ---------------- | -------- |
| Phase 1 — Snapshot export/import + tombstones (no OAuth) | 8/8              | complete |
| Phase 2 — CloudSyncPort + Google Drive adapter           | 6/6              | complete |
| Phase 3 — syncWithCloud merge engine                     | 7/7              | complete |
| Phase 4 — Hybrid sync UI                                 | 0/5              | blocked  |
| Phase 5 — Optional E2E encryption                        | 0/4              | blocked  |
| Verification & wrap-up                                   | 0/5              | blocked  |

### Blocked-phase reasons

- **Phase 4 — Hybrid sync UI** — Depends on Phase 3 (syncWithCloud merge engine) code. Phase 3 is now complete, so this is the next actionable implementation target.
- **Phase 5 — Optional E2E encryption** — Depends on Phase 4 (Hybrid sync UI) code, which is not yet complete.
- **Verification & wrap-up** — Depends on Phase 5 and all prior code phases being complete before final verification, coverage, docs, and changeset.

## Spec coverage

| Capability           | Requirements | Scenarios covered/total |
| -------------------- | ------------ | ----------------------- |
| spa-cloud-sync       | 10           | 12/29                   |
| spa-persistence-port | 2            | 4/4                     |

## Blockers

- Phase 4 (Hybrid sync UI) is blocked until Phase 3 (syncWithCloud merge engine) code lands. _(Phase 3 is now complete — this blocker is cleared and Phase 4 is actionable.)_
- Phase 5 (Optional E2E encryption) is blocked until Phase 4 (Hybrid sync UI) code lands.
- Phase 6 (Verification & wrap-up) is blocked until Phase 5 and all prior code phases are complete.
- No external blockers remain: Google Cloud OAuth Client ID is provisioned (tasks.md 2.2) and the `drive.appdata` scope is confirmed non-sensitive, so no Google verification gate exists for development.

## Next action

Start Phase 4 (Hybrid sync UI). With Phase 3 (syncWithCloud merge engine) complete, the remaining gap to user-facing sync is the trigger/UI layer: pull-on-app-open, debounced push after edits, a manual "Sync now" Settings control, offline/non-fatal degradation, and the connect/disconnect-account UI. These are the spa-cloud-sync scenarios still uncovered.

## This run

Implemented **Phase 3 — syncWithCloud merge engine** (tasks 3.1–3.7) TDD-style. All seven tasks in the `## 3.` group are done; the full suite and lint are green. (Phases 4–6 are out of scope for this run.)

**Files created:**

- `packages/workout-spa-editor/src/application/sync/merge-snapshots.ts` — pure LWW `mergeSnapshots(local, remote)`: per-record merge keyed by primary key, timestampless tables (`meta`/`usage`) merged whole-record by manifest `exportedAt`, tombstone union + suppression.
- `packages/workout-spa-editor/src/application/sync/merge-record-key.ts` — helpers: `TIMESTAMPLESS_TABLES`, per-table `recordKey` (handles non-`id` PKs like `usage.yearMonth`, `meta.key`, `coachingSyncState[source+profileId]`, etc.), `recordClock` (`updatedAt`→`createdAt`→0).
- `packages/workout-spa-editor/src/application/sync/merge-tombstones.ts` — `unionTombstones` (newest `deletedAt` per `[table+id]`), `tombstoneClocks`, `tombstoneKey`.
- `packages/workout-spa-editor/src/application/sync/sync-with-cloud.ts` — `syncWithCloud` orchestration: pull → export → merge → import → push, bounded retry (default 3) on revision conflict.
- `packages/workout-spa-editor/src/application/sync/prune-tombstones.ts` — `pruneTombstones` + `TOMBSTONE_RETENTION_MS` (90 days).
- Test files: `merge-snapshots.test.ts` (9), `sync-with-cloud.test.ts` (4), `prune-tombstones.test.ts` (2).

**Files changed:**

- `packages/workout-spa-editor/src/application/sync/import-snapshot.ts` — now prunes tombstones on import via injectable `now`.
- `packages/workout-spa-editor/src/application/sync/import-snapshot.test.ts` — added prune-on-import test.
- `openspec/changes/google-drive-cross-device-sync/tasks.md` — ticked 3.1–3.7.

**Implementation notes:** The in-memory `CloudSyncPort` fake rejects a stale `expectedRevision` by throwing; `syncWithCloud`'s bounded retry catches and re-pulls/re-merges, which satisfies the "stale revision triggers re-merge" scenario. The real Drive adapter (Phase 2) was not modified. The R-AppDexieImport / zustand-writethrough guard passes (no new application file imports `dexie-database`).

**Quality-gate result:** **PASS.**

- Tests: **PASS** — `pnpm --filter @kaiord/workout-spa-editor test` exit 0, 548 test files / 4382 tests passed (63s); sync suite 22/22.
- Build: **PASS** — `pnpm --filter @kaiord/workout-spa-editor build` exit 0 (~600ms).
- Lint (`pnpm lint`): **PASS** — exit 0, 0 errors.
- Mechanical guards (`pnpm test:scripts`): **PASS** — exit 0, 485 tests / 71 suites, 0 fail.

**Advisory warnings (non-fatal, gates still green; violate the repo's documented zero-warning policy, left unfixed per instructions):**

- `no-magic-numbers` at `merge-snapshots.test.ts:118:49` — 'No magic number: 9'.
- `no-magic-numbers` at `prune-tombstones.test.ts:26:69` — 'No magic number: 1000'.
- `[INEFFECTIVE_DYNAMIC_IMPORT]` — `OnboardingTutorial.tsx` is dynamically imported by `AppTutorial.tsx` but also statically imported by `HelpSection.tsx` and `use-onboarding-tutorial.ts`; the dynamic import will not move the module into another chunk.

## Uncovered scenarios

The following spec scenarios have no executable test coverage yet (covered=false):

### spa-cloud-sync (17 of 29 uncovered)

- Google Drive appDataFolder adapter — No backend dependency — No test asserts the deployment property "functions from static hosting with only an OAuth Client ID, no server". `drive-rest.test.ts` shows client-side fetch + token usage (implying no backend) but does not assert the absence of a server-side component. Unverified architectural/deployment claim.
- Hybrid sync triggers — Pull on app open — No test wires a pull-on-app-open trigger after persistence boot; no React hook or boot integration references `syncWithCloud`.
- Hybrid sync triggers — Debounced push after edits — No debounced-push trigger exists or is tested; no sync hook/scheduler is present.
- Hybrid sync triggers — Manual sync available (Sync now) — No Settings "Sync now" control or component exists; nothing asserts a manual full pull-merge-push cycle reporting success/failure.
- Hybrid sync triggers — Offline degradation (non-fatal failure) — No test asserts non-fatal handling of an offline/failed Drive call with the local DB remaining usable; no trigger layer to degrade gracefully.
- Optional E2E encryption — Encrypted snapshot is unreadable without the passphrase — No snapshot-encryption module exists; `syncWithCloud`/export never encrypts tables/tombstones; `manifest.encrypted` is always false in tests.
- Optional E2E encryption — Receiving device prompts for the passphrase — No passphrase-prompt logic or UI exists; nothing tests gating import on successful decryption when `manifest.encrypted` is true.
- Optional E2E encryption — One-time plaintext warning for AI keys — No one-time warning is implemented or tested for connecting an account / first enabling sync with AI keys present and encryption off.
- Account connection UI — Connect an account — OAuth consent at the GIS layer is tested, but there is NO Settings UI that runs the consent flow and shows the account as connected with sync enabled.
- Account connection UI — Disconnect stops sync but keeps data — No disconnect control or test; `gis-token-client` exposes `revoke` in the stub but no test asserts disconnect stops triggers while leaving the local DB intact, and no sync-trigger layer exists to stop.

> Newly covered this run (merge engine — previously uncovered, now green): Last-write-wins — Newer local edit wins / Newer remote edit wins / Record present on only one side is kept / Timestampless table merges whole-record; Deletions propagate — Delete on device A removes the record on device B / Re-creation newer than the tombstone wins; Optimistic concurrency — Stale revision triggers re-merge / Clean revision pushes directly. Plus the Phase 1–2 CloudSyncPort, Google Drive adapter, SnapshotPort, and full-database snapshot scenarios.

### spa-persistence-port (0 of 4 uncovered)

All 4 scenarios are covered: Deleting a workout records a tombstone; Tombstone and delete are atomic; Call sites are unchanged; Additive upgrade on existing database.
