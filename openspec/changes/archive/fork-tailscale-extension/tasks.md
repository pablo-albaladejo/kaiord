# Tasks: Fork tailscale-lambda-extension

## 1. Fork and setup

- [x] 1.1 Fork `rehanvdm/tailscale-lambda-extension` to `pablo-albaladejo/tailscale-lambda-extension`
- [x] 1.2 Clone fork locally
- [x] 1.3 Create branch `feature/advertise-tags-exit-node`
- [x] 1.4 Run `npm install` and verify build works (`npx projen build`)
- [x] 1.5 Run existing tests to establish baseline

## 2. Shell script changes

- [x] 2.1 Extract `extensions/tailscale-extension` from zip for editing
- [x] 2.2 Add `TS_ADVERTISE_TAGS` support: pass `--advertise-tags` to `tailscale up` when set
- [x] 2.3 Add `TS_EXIT_NODE` support: pass `--exit-node` to `tailscale up` when set
- [x] 2.4 Add exit node readiness wait loop (poll `tailscale status --json`, max 10s)
- [x] 2.5 Add logging for new features (`[tailscale-extension] Setting exit node...`)
- [x] 2.6 Rebuild zip with modified script

## 3. CDK construct changes

- [x] 3.1 Decision: do NOT add props to construct (it only creates a Layer, not a Function) — document env vars in README instead
- [x] 3.2 Update README.md with new environment variables and usage examples
- [x] 3.3 Update API.md (auto-generated by Projen)
- [x] 3.4 Run `npx projen build` to regenerate project files

## 4. Tests

- [x] 4.1 Verify existing tests still pass
- [x] 4.2 Manual integration test: deploy Lambda with `--advertise-tags` and OAuth client key
- [x] 4.3 Manual integration test: deploy Lambda with `--exit-node` and verify routing

## 5. Publish fork

- [x] 5.1 Publish as GitHub release tarball (v0.1.1)
- [x] 5.2 Verify installation in kaiord via release tarball URL

## 6. PR to upstream

- [x] 6.1 Open issue upstream (rehanvdm/tailscale-lambda-extension#5)
- [x] 6.2 Push feature branch to fork
- [x] 6.3 Create PR to `rehanvdm/tailscale-lambda-extension` (PR #6)

## 7. Migrate kaiord

- [x] 7.1 Switch `@kaiord/infra` dependency to fork release tarball
- [x] 7.2 Add `TS_ADVERTISE_TAGS` env var to CDK stack
- [x] 7.3 Remove `tailscale-exit-node.ts` and its test
- [x] 7.4 Remove `ensureExitNode()` call and import from `handler.ts`
- [x] 7.5 Create OAuth client key in Tailscale admin (no expiry, `tag:lambda`)
- [x] 7.6 Update Secrets Manager with OAuth client secret
- [x] 7.7 Update deploy workflow with `--context tsAdvertiseTags`
- [x] 7.8 Test full flow: deploy → smoke test passing

## 8. Changeset

- [x] 8.1 Create changeset for `@kaiord/infra` (minor: switch to fork, remove workarounds)

## 9. Post-merge cleanup (when upstream merges PR)

- [ ] 9.1 Switch back to official `tailscale-lambda-extension` package
- [ ] 9.2 Archive this openspec change
