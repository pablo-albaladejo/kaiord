<!-- opsx-ship: chunking
PR 1 (core-snapshot-schema):     §2 (DTO + fingerprint + STALE constant + fixtures in @kaiord/core)
PR 2 (spa-push-pipeline):        §3 (SPA mapper, push adapter, triggers, Dexie sync state, PII guard extension)
PR 3 (bridge-action-handlers):   §4 (validators + action wiring), §5 (T2G storage permission), §7 (privacy-surface guard)
PR 4 (icon-pipeline):            §1 (spike), §6 (SVG master + build script + distinctness guards)
PR 5 (popup-css-and-garmin):     §8 (shared popup CSS + parity guard + STALE parity), §9 (Garmin popup rewrite)
PR 6 (train2go-popup):           §10 (T2G popup rewrite + parser coachName extraction)
PR 7 (verify-and-archive):       §11 (lint/changesets/validate), §12 (e2e verification + /opsx-archive)
-->

## 1. Spike — confirm wire facts and tooling before committing

- [x] 1.1 From a logged-in browser session, capture one live `/api/v2/profile/ping` JSON response from Train2Go and pin which field (if any) carries the coach/trainer name. Record the verdict in `design.md` Open Questions Q2 and proceed accordingly (extract it OR drop the sub-line entirely — do NOT add a new endpoint).
- [x] 1.2 Run `pnpm why sharp` and `pnpm why @napi-rs/canvas` from the repo root; document the chosen rasterizer in `design.md` Open Questions Q1. If neither is present transitively, prefer `sharp` as a direct repo-root devDependency (script-only, NOT extension-shipped).
- [x] 1.3 Sanity-check that the existing Garmin `ping` envelope (`/workout-service/workouts?start=0&limit=1`) carries any user-display field; if not, the popup omits the display-name sub-line. Record verdict.
- [x] 1.4 Audit `packages/workout-spa-editor/src/adapters/bridge/bridge-transport.ts` `sendBridgeMessage(extensionId, message: unknown)` to confirm it accepts the new `{ action: "profile-snapshot" | "profile-snapshot-clear", ... }` shape without any port-surface change. If a port change IS required, halt and add a `spa-bridge-protocol` delta requirement BEFORE proceeding.

## 2. Snapshot DTO and constants in `@kaiord/core` (failing tests first)

- [x] 2.1 Add `profileSnapshotSchema` and `ProfileSnapshot` type at `packages/core/src/types/profile-snapshot.ts` with Zod validation matching the spec DTO. Cap `JSON.stringify(snapshot).length` (UTF-16 code-unit count, no replacer/spacer) at 8192 — validator-level rejection. Build the snapshot in the field order documented in the DTO requirement so the serialized length is deterministic. Also export `STALE_SNAPSHOT_THRESHOLD_DAYS = 7` and a pure `fingerprintSnapshot(snapshot): string` (FNV-1a 32-bit hex of canonical JSON of `(profile.id, snapshot fields excluding generatedAt)`). Export the schema, the `ProfileSnapshot` type, the constant, and the fingerprint function (four total) from the core package barrel (`packages/core/src/index.ts`).
- [x] 2.1.a Unit-test `fingerprintSnapshot`: identical content + different `generatedAt` → identical fingerprint; differing FTP → different fingerprint; deterministic across multiple invocations on the same input.
- [x] 2.2 Write `profile-snapshot.test.ts` in `@kaiord/core` covering: valid v1 payload accepted, payload missing `schemaVersion`/`profile.name`/`generatedAt` rejected, unknown `activeSport` rejected, `bodyWeight ≤ 0` rejected, oversized payload (>8 KiB) rejected, prototype-pollution input (`{ __proto__: {...}, ... }`) rejected, **nested** prototype-pollution rejected, **partial-zone** payload accepted (e.g., `running.lthr` set, `running.thresholdPaceSecPerKm` absent). Pin the canonical byte length of the baseline fixture for deterministic boundary tests. **Tests fail first**, then schema is added until they pass.
- [x] 2.3 Add a shared positive/negative fixture set at `packages/core/src/test-utils/profile-snapshot-fixtures.ts` (re-exported from `@kaiord/core/test-utils`) — used by both the SPA mapper tests AND the bridge parity tests. Verify `packages/core/package.json`'s `exports` field exposes the `./test-utils` sub-path; update the barrel `packages/core/src/test-utils/index.ts` to re-export the new fixtures so consumers can `import { snapshotFixtures } from "@kaiord/core/test-utils"`. Run `pnpm -r build` and `pnpm -r test` to verify resolution from both the SPA and the bridge test packages.
- [x] 2.4 Add a pure mapper `profile-to-snapshot.ts` in `@kaiord/workout-spa-editor` (and `profile-to-snapshot.test.ts`) that derives a `ProfileSnapshot` from the SPA's domain `Profile` + active-sport selection; covers cycling/running/swimming and partial-zone configurations. Imports the schema/type from `@kaiord/core`.

## 3. SPA push pipeline (failing tests first)

- [x] 3.1 In the SPA's bridge integration layer, add a `pushProfileSnapshot(bridge, snapshot)` adapter call that uses `sendBridgeMessage` (no new port). Unit test mocks the transport and asserts the action message shape: `{ action: "profile-snapshot", snapshot }`. Add a sibling `clearProfileSnapshot(bridge)` for the right-to-be-forgotten path.
- [x] 3.2 Implement SPA-side de-duplication via content fingerprint: the push adapter persists, per `bridgeId`, `lastSuccessfulFingerprint` of the most recent successful push **in the SPA's bridge sync-state Dexie table** (the same store that holds `pendingClear` — see 3.7). If the next push has the same fingerprint, skip without calling the transport AND without charging the per-bridge rate limiter. Update the persisted fingerprint ONLY on `ok: true` ack — transport timeout, `ok: false` validation/storage failure, and pre-redesign `Unknown action` responses SHALL leave the previous fingerprint in place. Unit tests: (a) two snapshots with identical content but different `generatedAt` emit one transport call; (b) two snapshots with different FTP emit two transport calls; (c) a transport timeout does not update the fingerprint, so the next push with the same content goes through.
- [x] 3.3 Wire trigger points and unit-test each in isolation:
  - 3.3.a Active-profile mutation effect — pushes a fresh snapshot to every VERIFIED bridge after the mutation commits.
  - 3.3.b Active-profile creation (`null → newId`) — pushes a fresh snapshot to every VERIFIED bridge.
  - 3.3.c Bridge-VERIFIED transition — the bridge registry/state machine emits an event (or exposes a hook) that triggers a one-time snapshot push (or, if a `pendingClear` flag is set for that bridge, sends the clear FIRST, then the new snapshot if any).
  - 3.3.d Active-profile deletion effect — sends `profile-snapshot-clear` to every VERIFIED bridge instead of a fresh snapshot. For UNAVAILABLE bridges, sets a `pendingClear` flag in sync state for that bridge.
- [x] 3.4 Assert non-blocking behavior with a unit test: the user-visible profile-save flow resolves before the snapshot push promise.
- [x] 3.5 Assert rate-limit pass-through with a unit test: when the existing per-bridge rate limiter would block, the push is silently skipped (no toast).
- [x] 3.6 Assert UNAVAILABLE bridges are skipped: a snapshot push fired during a profile mutation does NOT reach a bridge currently in UNAVAILABLE status.
- [x] 3.7 Implement the `pendingClear` sync-state flag and exercise it: deleting the active profile while a bridge is UNAVAILABLE sets the flag; the next VERIFIED transition emits `profile-snapshot-clear` BEFORE any new push, then clears the flag. Add a Dexie schema version bump for the bridge sync-state table: new columns `pendingClear: boolean` (default `false`) and `lastSuccessfulFingerprint: string | null` (default `null`); migration retains existing rows untouched apart from the defaults.
- [x] 3.8 Assert that a transport timeout (no response from the bridge) does NOT mark the push as the most-recent-successful: the next mutation must trigger another push for the same `(profile.id, generatedAt)` key.
- [x] 3.9 Extend the existing R-PIIInterpolation rule (defined in `scripts/check-no-pii-leakage.mjs`, see CLAUDE.md — first argument must be a bare string literal or top-level SCREAMING_SNAKE_CASE constant referencing a literal) so its glob covers: `packages/workout-spa-editor/src/adapters/bridge/**`, any new `packages/workout-spa-editor/src/lib/profile-snapshot/**`, AND **both bridges' `popup.js`, `popup.html` inline `<script>` blocks, and `background.js`**. The guard re-uses the existing rule logic — do NOT invent a parallel matcher. Add fixture tests that DEMONSTRABLY exercise the rule:
  - Negative fixture A: `console.log(\`profile: ${snapshot.profile.name}\`)` in a bridge popup.js — interpolated first arg, MUST fail the lint job.
  - Negative fixture B: `console.log(snapshot)` in a bridge popup.js — bare-identifier first arg, MUST fail the lint job.
  - Positive fixture: `console.log("Loaded profile snapshot")` (no PII), MUST pass.
  - Each fixture is asserted with both "fixture present → lint fails" and "fixture removed → lint passes" so the test cannot silently pass on a broken rule.
    Wire into `pnpm test:scripts`.

## 4. Bridge action handlers — failing tests first

- [x] 4.1 In `packages/garmin-bridge/test/profile-snapshot.test.js`, add tests for the new actions: valid payload persists to `chrome.storage.local` with `receivedAt` and acks `data: { storedAt }`; invalid payload rejected; prototype-pollution (top-level AND nested) payload rejected with `Object.prototype` unmodified post-test; oversized payload (>8 KiB) rejected; storage-write failure surfaced as `retryable: false`; `profile-snapshot-clear` removes both `profileSnapshot` and `lastWeeklyRollup`; clear is idempotent; **disallowed-`sender.origin` payload rejected with `error: "Origin not permitted"`**; **`chrome.runtime.onMessageExternal` listener returns `true` so async `sendResponse` survives SW idle teardown**; no network request issued for either action (assert `garminFetch` is not called). Tests fail first.
- [x] 4.2 Implement the bridge-side structural validator in `packages/garmin-bridge/profile-snapshot.js` (plain JS) and export `validateSnapshot(input) → { ok, value | error }`. Validator MUST construct outputs via `Object.create(null)`, MUST reject own-keys `__proto__`/`constructor`/`prototype` using `Object.getOwnPropertyNames`, MUST NOT spread attacker input, AND MUST reject payloads whose `JSON.stringify(input).length` exceeds 8192 bytes.
- [x] 4.3 Wire `profile-snapshot` and `profile-snapshot-clear` actions in `packages/garmin-bridge/background.js` `handleAction` switch; ensure `sendResult` / `sendError` envelopes match the spec.
- [x] 4.4 Repeat 4.1–4.3 for `packages/train2go-bridge/` (test file, validator, background action wiring) including the `sender.origin` undefined-or-disallowed rejection test, the listener-returns-`true` test, and nested-prototype-pollution rejection. The "no network request issued" assertion uses `train2goFetch` (the T2G symbol corresponding to Garmin's `garminFetch`).
- [x] 4.5 Add a parity test (one per bridge) that loads the shared fixture set from `@kaiord/core/test-utils` and asserts the bridge validator's parsed output **deep-equals** the Zod-parsed output for every positive fixture (NOT just `accepts/rejects`); rejects the same negative fixtures (including top-level and NESTED prototype-pollution, partial-zone, oversized-payload). The deep-equal check ensures a future schema addition that the bridge's plain-JS validator silently strips fails the parity test. The parity test SHALL be a `devDependencies`-only consumer of `@kaiord/core`; verify each bridge's `package.json` keeps `@kaiord/core` out of `dependencies`.

## 5. Train2Go manifest gains the `storage` permission

- [x] 5.1 Update `packages/train2go-bridge/manifest.json` `permissions` from `["tabs"]` to `["tabs", "storage"]`. Update `packages/train2go-bridge/manifest.prod.json` to match.
- [x] 5.2 Update `packages/train2go-bridge/store-listing.md` and `packages/train2go-bridge/privacy-justification.md` with the storage justification: "Stores the active Kaiord profile snapshot locally so the popup can render athlete data without a network call. The snapshot is removed by the `profile-snapshot-clear` action when the user deletes their profile in Kaiord, AND fully removed when the extension is uninstalled (Chrome wipes the extension's `chrome.storage.local` namespace on uninstall)."
- [x] 5.3 Consolidate the manifest `permissions` array into the privacy-surface golden snapshot (task 7.1) so all CWS-relevant surface (`host_permissions`, `permissions`, `content_scripts.matches`, `externally_connectable.matches`, content-script `ALLOWED`, popup.js `fetch` URLs) lives in one golden file. The single guard `scripts/check-bridge-privacy-surface.mjs` covers all of them; no separate `check-bridge-permissions.test.mjs` is needed. Update `scripts/fixtures/bridge-privacy-surface.json` to include the new `permissions` field for both bridges.

## 6. Icon master, build script, and mechanical guards — failing tests first

- [x] 6.1 Author the shared SVG master at `packages/_shared/extension-icon/master.svg` with a clearly-marked accent color attribute (`fill="var(--accent, #007cc3)"` or a `data-accent` placeholder) that the build script substitutes.
- [x] 6.2 Add `scripts/build-extension-icons.mjs` that takes `--bridge garmin|train2go` and `--out <path>` and writes `icon{16,48,128}.png`. Implementation rasterizes via the rasterizer chosen in 1.2.
- [x] 6.3 Add `scripts/build-extension-icons.test.mjs` (node:test) covering: produces three PNGs of the expected dimensions; the accent color is reflected in the output (sample-pixel test against the SVG accent token); running for both bridges yields six files. **Tests fail first.**
- [x] 6.4 Add `scripts/check-extension-icons-distinct.mjs` that fails if (a) Garmin and Train2Go PNGs of the same size are below an inter-icon mean-color-delta threshold OR (b) at 16×16, the accent color (within ±15° hue tolerance of the bridge's accent token) constitutes less than 25% of non-transparent pixel mass. Co-located test asserts the guard fails on identical inputs, fails on a thin-stripe accent fixture, and passes on the produced PNGs.
- [x] 6.5 Wire `pnpm icons:build` and `pnpm lint:icons-distinct` in the root `package.json`. Add `pnpm lint:icons-distinct` to the lint job alongside the existing script-tests so the husky `pre-commit` hook and CI pick it up.
- [x] 6.6 Run `pnpm icons:build` and commit the regenerated PNGs. Verify by eye in the toolbar that the two icons are distinguishable at 16×16.

## 7. Privacy-surface mechanical guard

- [x] 7.1 Add `scripts/check-bridge-privacy-surface.mjs` and a co-located test that loads `manifest.json`, `manifest.prod.json` (specifically `permissions`, `host_permissions`, `content_scripts.matches`, `externally_connectable.matches`), and `content.js` `ALLOWED` exports for both bridges and compares them to a checked-in golden snapshot stored under `scripts/fixtures/bridge-privacy-surface.json`. Fails if any byte changes without the golden being updated in the same commit.
- [x] 7.2 Extend the same guard to grep `popup.js` for `fetch(` and `XMLHttpRequest` patterns and assert every URL argument is a relative path (no `http(s)://` scheme literal). Co-located test asserts the guard fails on a fixture popup.js with an absolute URL and passes on the redesigned popups.
- [x] 7.3 Generate the initial golden from the post-redesign state (the only difference vs pre-redesign is the new `storage` permission in the Train2Go manifests). Document the golden delta in the PR description so a reviewer can confirm the only CWS-relevant change is the `storage` addition.
- [x] 7.4 Wire into `pnpm test:scripts` so it runs in the existing lint job and pre-commit hook.

## 8. Shared assets package + popup CSS

- [x] 8.0 Set up the new `packages/_shared/` workspace top-level: add `packages/_shared/package.json` with `"private": true` and a stub `name: "@kaiord/internal-extension-assets"`; ensure `pnpm-workspace.yaml`'s `packages:` glob includes it; add a brief README explaining `_shared` holds non-publishable assets shared across extension packages (icon master, popup CSS master). Document the convention in repo `CLAUDE.md` so future contributors know when to use `_shared` vs a published `@kaiord/*` package.
- [x] 8.1 Author the shared structural CSS at `packages/_shared/popup/popup.css` with the layout tokens defined in `design.md` D6 (340 px width, typography, accent dot, 3-column header grid with reserved 16×16 refresh-slot, status pill, athlete-card 2-column micro-grid + 0/1/≥2 field-count rules, primary/secondary CTA, `visibility: hidden` for the refresh-icon during loading/failure). Color tokens (`--accent`, `--accent-hover`) are placeholders — each bridge's `popup.html` defines them.
- [x] 8.2 Add a `pnpm popup:sync` script (`scripts/sync-popup-css.mjs`) that copies the master CSS byte-for-byte into `packages/{garmin,train2go}-bridge/popup.css`.
- [x] 8.3 Add `scripts/check-popup-css-parity.test.mjs` (node:test) that asserts both bridge `popup.css` files are byte-identical to the shared master at `packages/_shared/popup/popup.css`. Wire into `pnpm test:scripts` so drift is caught at the lint job. **Test fails first** with intentionally-divergent fixtures.
- [x] 8.4 Add `scripts/check-bridge-stale-threshold-parity.test.mjs` (node:test) that imports `STALE_SNAPSHOT_THRESHOLD_DAYS` from `@kaiord/core` and greps each bridge's `popup.js` for the regex `/STALE_SNAPSHOT_THRESHOLD_DAYS\s*=\s*(\d+)/` (NOT a bare digit grep — that would spuriously match any `7` in the file). Asserts the captured value equals the imported constant. Fails if they diverge. Wire into `pnpm test:scripts`.

## 9. Garmin popup rewrite

- [x] 9.1 Add `packages/garmin-bridge/test/popup.test.js` (jsdom + chrome stubs) covering the spec scenarios: connected card, disconnected with retry, snapshot placeholder when missing, stale-snapshot placeholder, retry-only-on-failure, **ping/snapshot timeout → disconnected**, **rollup-only timeout → connected with rollup-unavailable region (NOT disconnected)**, `AbortController` aborts the popup-side await on both timeout AND `pagehide` — including the snapshot-read await (within the 1 s window), the ping await, and the rollup await — verified by closing the popup mid-flight in each phase, refresh-icon affordance triggers re-fetch, refresh-icon `visibility: hidden` during loading (preserves 16×16 slot), **`getBoundingClientRect()` of header title is byte-identical across loading/success/loading-via-refresh transitions** (no layout shift), ARIA attributes present, Retry button auto-focuses on failure, **"Updated <relative time>" formatting boundary cases**: 59 s → "just now", 60 s → "1 minute ago", 23 h 59 m → "23 hours ago", 24 h → "1 day ago", 6 d 23 h → "6 days ago", 7 d 0 h → stale-snapshot placeholder. **Athlete card field-count rendering**: 0 fields → placeholder, 1 field → single full-width cell, ≥2 fields → 2-column grid. No outbound paths beyond the existing allowlist subset, and no absolute URLs in `popup.js` `fetch` calls. **Tests fail first.**
- [x] 9.2 Rewrite `packages/garmin-bridge/popup.html` to the identity-card layout with blue accent token (`#007cc3`), references shared `popup.css` (synced via task 8.2), 3-column header grid with reserved 16×16 refresh-slot, status pill with text label + glyph, refresh-icon header button, single primary "Open editor" CTA + secondary "Open Garmin Connect" link, athlete card with "Updated <relative time>" line.
- [x] 9.3 Rewrite `packages/garmin-bridge/popup.js` with an `AbortController`-based auto-fetch pipeline: read `chrome.storage.local.get("profileSnapshot")` (1 s timeout) + send `ping` (3 s timeout) + render workout-library count from the existing ping envelope (no extra fetch, 3 s timeout). Wire `window.addEventListener("pagehide", () => controller.abort())`. Use `Intl.RelativeTimeFormat("en", { numeric: "auto" })` snapped to the design D4 table. No `List Workouts` button; only `Retry` on failure (with auto-focus). Define `OPEN_EDITOR_URL` as a build-time constant: `https://app.kaiord.com/` for the prod manifest variant, `http://localhost:5173/` for the dev variant — keyed off whether the active manifest contains localhost in `externally_connectable`. Vendor `STALE_SNAPSHOT_THRESHOLD_DAYS` as a plain-JS literal (kept in lockstep with `@kaiord/core` via the parity test in 8.4).
- [x] 9.4 Validate that the SPA's `parseManifestFromPing` continues to work (Zod still strips session fields). Run the existing bridge-registry tests in the SPA to confirm no regression.

## 10. Train2Go popup rewrite

- [x] 10.1 Add `packages/train2go-bridge/test/popup.test.js` covering the spec scenarios: connected card with optional coach sub-line, disconnected with retry, snapshot placeholder, stale-snapshot placeholder, weekly rollup auto-loaded from a single `read-week` (cache miss), cached rollup hit skips `read-week`, concurrent cache-miss permits both fetches (no coalescing), rapid popup close/re-open during in-flight rollup tolerates duplicate fetch, refresh-icon bypasses TTL, **ping/snapshot timeout → disconnected**, **rollup-only timeout (>8 s) → connected with "Rollup unavailable — try again"** (NOT disconnected), `AbortController` aborts the popup-side await on both timeout AND `pagehide` — including the snapshot-read await (within the 1 s window), the ping await, and the rollup await — verified by closing the popup mid-flight in each phase, refresh-icon `visibility: hidden` during loading (preserves 16×16 slot), **`getBoundingClientRect()` of header title byte-identical across loading/success/loading transitions** (no layout shift), ARIA attributes, Retry auto-focus, **"Updated <relative time>" boundary cases**: 59 s, 60 s, 23 h 59 m, 24 h, 6 d 23 h, 7 d 0 h. **Athlete card field-count rendering**: 0/1/≥2 fields. No outbound paths beyond the allowlist, and no absolute URLs in `popup.js` `fetch` calls. **Tests fail first.**
- [x] 10.2 Update `packages/train2go-bridge/parser.js` `parsePingJson` to extract a `coachName` field if the upstream payload exposes it (path confirmed in 1.1); add unit tests covering present and absent cases.
- [x] 10.3 Rewrite `packages/train2go-bridge/popup.html` to the identity-card layout with coral accent token (`#f74464`), references shared `popup.css` (synced via task 8.2), 3-column header grid with reserved 16×16 refresh-slot, status pill with text label + glyph, refresh-icon header button, single primary "Open editor" CTA + secondary "Open Train2Go" link, athlete card with "Updated <relative time>" line.
- [x] 10.4 Rewrite `packages/train2go-bridge/popup.js`: `AbortController`-based auto-fetch pipeline with differentiated phase timeouts (snapshot 1 s, ping 3 s, rollup 8 s) and write-through `lastWeeklyRollup` cache (5-min TTL); one-line rollup "<N> sessions planned · <M> done · workload <X>"; rollup-only timeout renders connected + "Rollup unavailable — try again" (NOT disconnected); `pagehide` listener wires `controller.abort()`; `Intl.RelativeTimeFormat("en", { numeric: "auto" })` for the "Updated" line snapped to the design D4 table; no `Read This Week` button; `Retry` only on ping/snapshot failure (with auto-focus); `OPEN_EDITOR_URL` keyed off the prod-vs-dev manifest variant exactly as in the Garmin popup; `STALE_SNAPSHOT_THRESHOLD_DAYS` vendored as a plain-JS literal (parity-tested via 8.4).

## 11. Spec lint, validation, and changesets

- [x] 11.1 Run `pnpm lint:specs` and fix any drift in the four deltas under `openspec/changes/bridge-popup-redesign/specs/`.
- [x] 11.2 Run `npx openspec validate bridge-popup-redesign --strict` and resolve any complaints.
- [x] 11.3 Add changesets via `pnpm exec changeset`: minor for `@kaiord/core` (new public type + schema + `STALE_SNAPSHOT_THRESHOLD_DAYS` constant), minor for `@kaiord/garmin-bridge` and `@kaiord/train2go-bridge` (new actions + popup redesign + T2G permission addition), patch for `@kaiord/workout-spa-editor` (snapshot push effect + pendingClear sync state). One changeset file is fine if all entries fit cleanly.

## 12. End-to-end verification

- [x] 12.1 Run `pnpm -r build && pnpm -r test && pnpm lint` from the repo root; expect zero errors and zero warnings per the project's quality bar.
- [ ] 12.2 Load both unpacked extensions in Chrome side-by-side. Confirm the toolbar icons are visually distinct at 16×16. Confirm both popups auto-render the identity card on open with the active SPA profile present, and show the placeholder when run without ever opening the SPA.
- [ ] 12.3 Trigger a profile mutation in the SPA (e.g., bump FTP) and observe the popup reflects it on next open without any manual refresh.
- [ ] 12.4 Force a disconnected state (close the upstream tab) and confirm the popup renders the red status pill with a `Retry` button only, the Retry button auto-focuses, and clicking it re-runs the auto-fetch.
- [ ] 12.5 Delete the active profile in the SPA and confirm both popups fall back to the placeholder state on next open (verifies the `profile-snapshot-clear` path).
- [ ] 12.6 Force the UNAVAILABLE-during-delete path: simulate a bridge being UNAVAILABLE (close its host tab), delete the active profile, then re-open the host tab so the bridge transitions back to VERIFIED; confirm the popup snapshot is cleared (pendingClear path) without ever showing the deleted profile's data.
- [ ] 12.7 Use Chrome DevTools to manually trigger a `profile-snapshot` external message with a `__proto__`-poisoned payload AND a nested-poisoned payload; confirm the bridge rejects both and `Object.prototype` is unmodified in the service-worker context.
- [ ] 12.8 Run `/opsx-verify` to walk every spec scenario in the four deltas against the implementation; resolve any gap before opening the PR.

## 13. Manual verification (deferred to user)

Tasks 12.2 through 12.8 require interactive Chrome browser testing
(loading unpacked extensions, observing popup state across SPA
profile mutations, triggering disconnect/reconnect cycles, manually
firing prototype-pollution payloads from DevTools). These are
documented as user-side verification steps; this PR ships the code
ready for them. Once verified, the tasks above can be checked and
the change archived via `/opsx-archive`.
