# Tasks: Fork tailscale-lambda-extension

## 1. Fork and setup

- [ ] 1.1 Fork `rehanvdm/tailscale-lambda-extension` to `pablo-albaladejo/tailscale-lambda-extension`
- [ ] 1.2 Clone fork locally
- [ ] 1.3 Create branch `feature/advertise-tags-exit-node`
- [ ] 1.4 Run `npm install` and verify build works (`npx projen build`)
- [ ] 1.5 Run existing tests to establish baseline

## 2. Shell script changes

- [ ] 2.1 Extract `extensions/tailscale-extension` from zip for editing
- [ ] 2.2 Add `TS_ADVERTISE_TAGS` support: pass `--advertise-tags` to `tailscale up` when set
- [ ] 2.3 Add `TS_EXIT_NODE` support: run `tailscale set --exit-node` after `tailscale up`
- [ ] 2.4 Add exit node readiness wait loop (poll `tailscale status --json`, max 10s)
- [ ] 2.5 Add logging for new features (`[tailscale-extension] Setting exit node...`)
- [ ] 2.6 Rebuild zip with modified script

## 3. CDK construct changes

- [ ] 3.1 Update `TailscaleLambdaExtensionProps` with `advertiseTags?` and `exitNode?` optional props
- [ ] 3.2 Update README.md with new environment variables and usage examples
- [ ] 3.3 Update API.md (auto-generated by Projen)
- [ ] 3.4 Run `npx projen build` to regenerate project files

## 4. Tests

- [ ] 4.1 Add unit test: construct passes environment variables when props provided
- [ ] 4.2 Add unit test: construct omits environment variables when props not provided
- [ ] 4.3 Verify existing tests still pass
- [ ] 4.4 Manual integration test: deploy Lambda with `--advertise-tags` and OAuth client key
- [ ] 4.5 Manual integration test: deploy Lambda with `--exit-node` and verify routing

## 5. Publish fork

- [ ] 5.1 Update package name to `@pablo-albaladejo/tailscale-lambda-extension` (scoped)
- [ ] 5.2 Publish to npm
- [ ] 5.3 Verify installation in kaiord: `pnpm --filter @kaiord/infra add @pablo-albaladejo/tailscale-lambda-extension`

## 6. PR to upstream

- [ ] 6.1 Revert package name change (keep as `tailscale-lambda-extension` for upstream)
- [ ] 6.2 Push feature branch to fork
- [ ] 6.3 Create PR to `rehanvdm/tailscale-lambda-extension` with description from design.md
- [ ] 6.4 Address review feedback

## 7. Migrate kaiord

- [ ] 7.1 Switch `@kaiord/infra` dependency from `tailscale-lambda-extension` to fork
- [ ] 7.2 Add `TS_ADVERTISE_TAGS` env var to CDK stack
- [ ] 7.3 Remove `tailscale-exit-node.ts` and its test
- [ ] 7.4 Remove `ensureExitNode()` call and import from `handler.ts`
- [ ] 7.5 Create OAuth client key in Tailscale admin (no expiry, `tag:lambda`)
- [ ] 7.6 Update Secrets Manager with OAuth client key
- [ ] 7.7 Update `SOLUTION.md` and `design.md` documentation
- [ ] 7.8 Test full flow: deploy → push workout → verify on Garmin Connect

## 8. Changeset

- [ ] 8.1 Create changeset for `@kaiord/infra` (minor: switch to fork, remove workarounds)

## 9. Post-merge cleanup (when upstream merges PR)

- [ ] 9.1 Switch back to official `tailscale-lambda-extension` package
- [ ] 9.2 Remove scoped fork from npm (optional)
- [ ] 9.3 Archive this openspec change
