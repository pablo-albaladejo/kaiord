<!-- opsx-ship: chunking
PR-1 (ci-failure-bot):           §1, §2 — Phase 1. Stops the active leak. Independently revertable.
PR-2 (archive-followups-guard):  §3, §4 — Phase 2. Prevents future leaks. Opens only after PR-1's canary inspection succeeds.

PR independence — explicit dependency map:
  PR-1: independent. Touches scripts/ci-failure-issue.{mjs,test.mjs}, .github/workflows/ci.yml, .github/workflows/ci-issue-bot-success.yml. No openspec/ or package.json edits beyond test-script registration.
  PR-2: depends on PR-1 having shipped AND its canary inspection having succeeded (per design D8). PR-2 touches scripts/check-archive-followups.{mjs,test.mjs}, openspec/SPEC_TEMPLATE.md, package.json (lint:archive-followups script + husky chain), and the 3 archived tasks.md files for backfill. No overlap with PR-1's surface.

Phase 0 preflight (before PR-1): the DRI runs `gh issue close 438 440 442` with one-liner pointing at the green main run. Done as part of opening PR-1; not a separate PR.
-->

## 1. PR-1 §1 — `scripts/ci-failure-issue.mjs` extraction (TDD red → green)

- [x] 1.1 `scripts/ci-failure-issue.test.mjs` lands first with 12 failing tests covering every branch in the spec, plus 2 canary-disambiguation tests and 3 parseFooter unit tests (17 total). Tests use a `fakeDeps` queue (gh CLI stub) mirroring `scripts/cws-notify-issue.test.mjs`. Branches covered: (1) no-open-issue + create → opens with footer, (2) open-issue + create → dedupe-comment, (3) no-open-issue + close → no-op, (4) open-issue + close on fully-green run (`anyJobsSkipped:false`) → close + audit, (5) open-issue + close on partial-green run (`anyJobsSkipped:true`) → skipped: jobs-skipped-on-green-run, (6) script-level dedupe (sequential calls; second sees first's issue and comments), (7) missing-footer + close → skipped: missing-footer, (8) malformed footer JSON + close → skipped: malformed-footer, no throw, (9) staleness re-check (list returns open; pre-close re-fetch returns closed → skipped: race-closed), (10) footer with `schema: 2` + close → skipped: unknown-schema, (11) footer with `failed-jobs: [...]` and no `schema:` line → treated as schema 1, closes like (4), (12) `--canary` flag on create → issue title prefixed with `[CANARY]`, label `canary` added, footer `failed-jobs: ["canary-job"]`. Plus: canary-create ignores existing canary issues; canary issues are not auto-closed by green runs (skipped: canary-issue); parseFooter unit tests for null/canonical/non-array.
- [x] 1.2 `scripts/ci-failure-issue.mjs` (CLI entry) + `scripts/ci-failure-issue-helpers.mjs` (logic; scripts are exempt from the 100-line cap per `eslint.config.js` ignores) implement the contract from `design.md` D2/D2b/D2c/D3/D4. Entry-point check via `pathToFileURL(process.argv[1]) === import.meta.url`. Exposes `--canary` flag. Reads `GH_TOKEN`, `GITHUB_REPOSITORY`, `GITHUB_SHA`, `GITHUB_ACTOR`, `GITHUB_RUN_ID`, `GITHUB_SERVER_URL`, `CI_ISSUE_BOT_ENABLED` from env. CLI usage: `create '<jobs-json>' [--canary]` and `close <true|false>` (the close arg is the `anyJobsSkipped` boolean per D2b's coarse rule). Uses `gh` CLI via `execFileSync` (no Octokit dep) mirroring `scripts/cws-notify-issue.mjs`.
- [x] 1.3 `pnpm test:scripts` green; 17 new tests pass; no other script test regresses (301/301 total).

## 2. PR-1 §2 — Workflow rewiring + kill-switch + canary

- [x] 2.1 `.github/workflows/ci.yml` `notify-failure` job updated: inline `actions/github-script@v9` block replaced by (a) a sparse `actions/checkout@v4` step (scripts only), (b) a bash assemble step emitting `failed-jobs` JSON keyed by job-id (lint, check-links, typecheck, test, test-cli, test-frontend, e2e-frontend, e2e-prod-base, build, round-trip), (c) `node scripts/ci-failure-issue.mjs create '<json>'`. Job-level `if:` extended with `vars.CI_ISSUE_BOT_ENABLED != 'false'` (kill-switch). Job-level `concurrency: { group: ci-issue-bot-${{ github.ref }}, cancel-in-progress: false }`. `permissions: { issues: write }` preserved.
- [x] 2.2 New `.github/workflows/ci-issue-bot-success.yml` listens on `workflow_run` of CI, `branches: [main]`, conclusion `success`. Same kill-switch + shared concurrency group + permissions. Three steps: sparse checkout; "detect skipped jobs" via `gh api repos/$REPO/actions/runs/$RUN_ID/jobs --jq '[.jobs[] | select(.conclusion == "skipped" and .name != "auto-merge")] | length'` → exposes `any` (true|false) as a step output; `node scripts/ci-failure-issue.mjs close <any>` invokes the v1 close-rule (D2b) — the `anyJobsSkipped` boolean is the entire input.
- [x] 2.3 New `.github/workflows/ci-issue-bot-canary.yml` triggered by `workflow_dispatch` only. Kill-switch + `permissions: { issues: write }`. Sparse checkout + `node scripts/ci-failure-issue.mjs create '["canary-job"]' --canary`. The script's `--canary` flag prepends `[CANARY]` to the title, adds the `canary` label, and writes footer `failed-jobs: ["canary-job"]`. The close-pass treats `"canary-job"` in `failed-jobs` as `skipped: canary-issue` (D2c) so the canary issue persists until DRI manual close.
- [x] 2.4 Phase 0 preflight executed: `gh issue close 438 440 442` with comment "Auto-closing: main green at f60327c9; superseded by ci-failure-bot rework in PR #462."
- [ ] 2.5 Canary acceptance: PR-1's PR description states that the DRI will inspect the first natural red-main issue within 24h of merge OR force the synthetic canary by triggering `ci-issue-bot-canary` via `workflow_dispatch` after 7 days of green main. Acceptance check on the PR description before merge.
- [x] 2.6 Changeset added at `.changeset/repo-hygiene-tooling-ci-failure-bot.md` (chore-class, no package bumps; repo-tooling change). Commit message follows conventional commits.

## 3. PR-2 §3 — `> Deferred to: #N` marker + lint script (TDD red → green)

- [x] 3.1 `openspec/SPEC_TEMPLATE.md` updated with paragraph 6 documenting the `> Deferred to: #N` marker shape and placement under the existing checkbox. `AGENTS.md` updated with an operational paragraph describing the marker alongside the existing `> Completed:` archive convention. `pnpm lint:specs` continues to pass.
- [x] 3.2 `scripts/check-archive-followups.test.mjs` lands first with 7 failing tests: zero-deferral → pass+silent; below-cap (4) → pass+log; at-cap (6) → fail with archive named in stderr; malformed marker (URL form) → fail with parse error; malformed (no hash) → fail; multi-archive mixed scenario → only over-cap fails; missing tasks.md → silently skipped. Mirrors the temp-root harness pattern from `scripts/check-archive-dates.test.mjs`.
- [x] 3.3 `scripts/check-archive-followups.mjs` implements the contract. Architectural mirror of `scripts/check-archive-dates.mjs`: identical entry-point check (`pathToFileURL(process.argv[1]) === import.meta.url`), exported `checkArchiveFollowups()` function, violation-collection-then-exit-with-code pattern. Walks `openspec/changes/archive/*/tasks.md`, counts `> Deferred to: #N` markers per archive, fails at threshold ≥ `ABSOLUTE_DEFERRAL_CAP` (exported constant). Allows leading whitespace on marker lines (markers are indented under their parent checkbox).
- [x] 3.4 `pnpm test:scripts` green; 7 new tests pass.

## 4. PR-2 §4 — `package.json` wiring + backfill + first-fail signal

- [x] 4.1 `package.json`: `"lint:archive-followups": "node scripts/check-archive-followups.mjs"` added as a sibling of `lint:archive` and `lint:archive-index`. Umbrella `lint` script (explicit `&&` chain — not `run-s`) extended with `pnpm lint:archive-followups` immediately after `pnpm lint:archive-index`.
- [x] 4.2 Backfilled `2026-05-01-calendar-coaching-redesign/tasks.md` with 5 markers (one per follow-up issue, attached to the FIRST deferred sub-task in each set): `#431` on §8.5, `#432` on §9.1, `#433` on §10.3, `#434` on §11.1, `#435` on §12.4a. Diff is marker-only; no checkbox flips, no text edits. The marker convention is one per ISSUE not one per sub-task; multi-sub-task deferrals to the same issue annotate the entry-point sub-task only.
- [x] 4.3 `2026-05-02-calendar-coaching-redesign-completion/tasks.md`: 0 markers added. The spec-sync deferral (#460) was captured in proposal.md / design.md prose only — the original tasks.md has no `[ ]` task line for spec-sync, and adding one retroactively would be scope rewriting (not annotation). Spec table updated to reflect this.
- [x] 4.4 `2026-05-02-fix-coaching-dialog-rules-of-hooks/tasks.md`: 0 markers added. The 3 follow-up issues (#450, #451, #454) were filed at archive-time per /opsx-ship convention but no `[ ]` task lines correspond. Same scope-rewriting argument as 4.3.
- [x] 4.5 Strategy (a) shipped per default: `ABSOLUTE_DEFERRAL_CAP = 6` so calendar-redesign's 5 markers pass cleanly. PR-2 description states the strategy + commits to a same-day follow-up PR lowering the cap to 5 to convert the 5-deferral signal into the intended first-fail.
- [ ] 4.6 Tracked-tech-debt follow-up issue filed at PR-2 ship-time: "Add machine-readable shape to `tasks.md` so `check-archive-followups` can encode the deferral-ratio invariant." Title prefix `chore(openspec):`. Issue body cross-references design.md D6 sunset trigger.
- [x] 4.7 Local lints clean on the post-backfill state with cap = 6 (per strategy (a)): `pnpm lint:archive-followups` passes with `2026-05-01-calendar-coaching-redesign: 5 deferrals (cap 6)` log line. Strategy choice recorded in PR description.
- [ ] 4.8 Changeset added; commit message `feat(openspec): archive-followups marker + lint guard + calendar-redesign backfill`; PR-2 ready for review.
