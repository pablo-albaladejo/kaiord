# Lifecycle Dashboard — google-drive-cross-device-sync

> Generated by openspec-lifecycle workflow

Artifacts complete: **yes**. Phase 1 (local snapshot export/import + tombstones, no OAuth) has been **regenerated and committed** and is fully green (539 files / 4342 tests passing; build, lint, and mechanical guards all clean). It was lost once when uncommitted code was discarded on a branch switch; this time it is committed immediately. Phases 2–5 remain gated on a Google Cloud OAuth Client ID and the `drive.appdata` sensitive-scope verification.

## Phase progress

| Phase                                                    | Tasks done/total | Status   |
| -------------------------------------------------------- | ---------------- | -------- |
| Phase 1 — Snapshot export/import + tombstones (no OAuth) | 8/8              | complete |
| Phase 2 — CloudSyncPort + Google Drive adapter           | 0/6              | blocked  |
| Phase 3 — syncWithCloud merge engine                     | 0/7              | blocked  |
| Phase 4 — Hybrid sync UI                                 | 0/5              | blocked  |
| Phase 5 — Optional E2E encryption                        | 0/4              | blocked  |
| Verification & wrap-up                                   | 0/5              | pending  |

### Blocked-phase reasons

- **Phase 2** — Requires a Google Cloud OAuth Client ID (Web) and the `drive.appdata` sensitive-scope setup before the GIS auth and Drive REST adapter can be exercised. External configuration, not code.
- **Phase 3** — Depends on Phase 2 (CloudSyncPort/Google Drive adapter), which is gated on the Google OAuth Client ID and `drive.appdata` verification.
- **Phase 4** — UI wiring of pull-on-open, debounced auto-push, and the Settings connect/disconnect flow requires the OAuth-gated Google Drive adapter from Phases 2–3.
- **Phase 5** — Encrypts the snapshot before push and decrypts on pull, so it depends on the OAuth-gated cloud sync flow (Phases 2–4).

## Spec coverage

| Capability           | Requirements | Scenarios covered/total |
| -------------------- | ------------ | ----------------------- |
| spa-cloud-sync       | 10           | 0/29                    |
| spa-persistence-port | 2            | 0/4                     |

## Blockers

- Phases 2–5 are gated on an external Google Cloud OAuth Client ID (Web app, with authorized JS origins for the SPA domain + localhost) — a configuration step outside the codebase.
- The `drive.appdata` scope is classified "sensitive" by Google and requires production verification (free but a review process) before serving more than ~100 users; test-user mode works in the interim. Documented as an ops caveat, not a code task.
- Phase 1 implementation was previously green (4335 tests passing) but the files were uncommitted and lost during a branch switch on `fix/spa-editor-redesign-e2e`; the code must be regenerated. No implementation or tests currently exist in `packages/workout-spa-editor`.

## Next action

Implement Phase 1 (task group 1: Snapshot export/import + tombstones, 0/8 done) — the only unblocked pending group, with no external OAuth dependency. Re-run the implement workflow for phase 1 to regenerate the lost code following the `SnapshotPort` shape in `design.md`: domain `Snapshot`/`RemoteSnapshot`/`SnapshotManifest` types, the v18→v19 Dexie tombstones table + migration, the `PersistencePort` Tombstone repo + `withTombstones` delete-decorator, and the pure `exportSnapshot`/`importSnapshot` use cases with round-trip tests. Phases 2–5 stay blocked pending a Google Cloud OAuth Client ID and `drive.appdata` sensitive-scope verification.

## This run

Phase 1 (snapshot export/import + tombstones, no OAuth) was implemented; all 8 tasks (1.1–1.8) were ticked. (This code was subsequently lost on a branch switch and must be regenerated — see Blockers.)

**Files created:**

- `src/types/snapshot.ts` — `Snapshot`, `RemoteSnapshot`, `SnapshotManifest`, `SnapshotTables`, `Tombstone` domain types (1.1)
- `src/ports/tombstone-repository.ts` — `TombstoneRepository` port (put/get/list/prune) (1.4)
- `src/ports/snapshot-port.ts` — `SnapshotPort` (schemaVersion/exportTables/importTables/listTombstones/replaceTombstones) (1.6)
- `src/adapters/dexie/dexie-tombstone-repository.ts` — Dexie tombstone repo (1.4)
- `src/adapters/dexie/dexie-snapshot-port.ts` — Dexie SnapshotPort enumerating `db.tables` (1.6)
- `src/adapters/dexie/dexie-snapshot-port.test.ts` — round-trip + encrypted-aiProviders decrypt test (1.7)
- `src/adapters/dexie/dexie-v19-migration.test.ts` — v18→v19 migration test (1.3)
- `src/adapters/with-tombstones.ts` + `.test.ts` — delete-decorator wiring tombstone writes into the same transaction (1.5)
- `src/application/sync/export-snapshot.ts` + `.test.ts`, `import-snapshot.ts` + `.test.ts` — pure use cases depending only on `SnapshotPort` (1.6/1.7)
- `src/test-utils/in-memory-tombstone-repository.ts` + `.test.ts`, `in-memory-snapshot-port.ts` — in-memory fakes (1.4/1.6)

**Files changed:**

- `src/adapters/dexie/dexie-schemas.ts` — added `tombstones` table, schema `v19` (1.2). Deliberately did NOT index `profileId` (only `[table+id]`, `table`, `deletedAt`) so `isPerProfileTable` does not classify tombstones as a profile-cascade target — tombstones must survive a profile delete to propagate.
- `src/adapters/dexie/register-kaiord-versions-v10-plus.ts` + `register-kaiord-versions.ts` — registered v19 (additive) (1.2)
- `src/ports/persistence-port.ts` — added `tombstones: TombstoneRepository` (1.4)
- `src/adapters/dexie/dexie-persistence-adapter.ts` + `src/test-utils/in-memory-persistence.ts` + `in-memory-persistence-snapshot.ts` — wired the tombstone repo into both PersistencePort assemblies (1.4)
- `src/adapters/dexie/dexie-v17-migration.test.ts` — updated hardcoded head schema version from 18 to 19 (the only collateral test break from the schema bump).

**Quality-gate result:**

- Tests: **PASS** — 539 files, 4342 tests passing (`pnpm --filter @kaiord/workout-spa-editor test`).
- Lint: **PASS** — 0 errors, 0 warnings (full repo lint exit 0).
- Mechanical guards: **PASS** — `pnpm test:scripts` 485 passing (R-AppDexieImport clean — sync use cases import only ports).
- Build: **PASS** (after follow-up fix) — the workflow left a `TS2589` error in `dexie-snapshot-port.ts` from Dexie's recursive `transaction` overloads. Fixed by calling `transaction` as a method on a `{ transaction: DexieTxScope }`-narrowed cast of `db` (the same pattern as `dexie-persistence-adapter.ts`), which both cuts the overload depth and keeps `this` bound to `db`. `tsc -b && vite build` now green and the new Dexie-level `dexie-snapshot-port.test.ts` passes.

## Uncovered scenarios

### spa-cloud-sync (24 of 29 uncovered)

- CloudSyncPort interface — Application code depends on the port
- CloudSyncPort interface — Pull returns null when no remote snapshot exists
- CloudSyncPort interface — Push returns the new revision identifier
- Google Drive appDataFolder adapter — First push creates the canonical file
- Google Drive appDataFolder adapter — Subsequent push updates the same file
- Google Drive appDataFolder adapter — Pull reads file and revision together
- Google Drive appDataFolder adapter — No backend dependency
- Last-write-wins conflict resolution — Newer local edit wins
- Last-write-wins conflict resolution — Newer remote edit wins
- Last-write-wins conflict resolution — Record present on only one side is kept
- Last-write-wins conflict resolution — Timestampless table merges whole-record
- Deletions propagate via tombstones — Delete on device A removes the record on device B
- Deletions propagate via tombstones — Re-creation newer than the tombstone wins
- Optimistic concurrency on push — Stale revision triggers re-merge
- Optimistic concurrency on push — Clean revision pushes directly
- Hybrid sync triggers — Pull on app open
- Hybrid sync triggers — Debounced push after edits
- Hybrid sync triggers — Manual sync available
- Hybrid sync triggers — Offline degradation
- Optional end-to-end encryption — Encrypted snapshot is unreadable without the passphrase
- Optional end-to-end encryption — Receiving device prompts for the passphrase
- Optional end-to-end encryption — One-time plaintext warning for AI keys
- Account connection UI — Connect an account
- Account connection UI — Disconnect stops sync but keeps data

> Covered (5 of 29): SnapshotPort interface — Snapshot use cases depend on the port, not Dexie; SnapshotPort interface — Adapter enumerates all tables generically; Full-database snapshot — Snapshot includes all tables; Full-database snapshot — Export then import round-trips; Full-database snapshot — AI provider rows survive the round-trip. (These were proven by the now-lost Phase 1 code and must be re-established on regeneration.)

### spa-persistence-port (0 of 4 uncovered)

All 4 scenarios were covered by the Phase 1 implementation: Deleting a workout records a tombstone; Tombstone and delete are atomic; Call sites are unchanged; Additive upgrade on existing database. Coverage must be re-established when the lost Phase 1 code is regenerated.
