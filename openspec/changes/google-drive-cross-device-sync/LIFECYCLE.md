# Lifecycle Dashboard — google-drive-cross-device-sync

> Generated by openspec-lifecycle workflow

> ⚠️ **Phase 1 code lost — regeneration required.** Phase 1 was implemented and fully green once (4335 tests passing; build/lint/guards clean) but the files were uncommitted on the `fix/spa-editor-redesign-e2e` branch and were discarded during a later branch switch (untracked + unstaged → unrecoverable from git). The OpenSpec artifacts and the lifecycle workflow survived and are committed. To rebuild Phase 1 deterministically, run `Workflow openspec-lifecycle { mode: "implement", phase: "1" }` from a dedicated feature branch, then commit immediately. The "This run" section below records exactly what was built, including the two post-workflow fixes (TS2589 cast + magic-number constants) that the regeneration should reapply.

OpenSpec change for cross-device sync of the workout-spa-editor's Dexie database via the user's Google Drive `appDataFolder`. All four artifacts (proposal, design, specs, tasks) are complete. Phase 1 (local snapshot export/import + tombstones, no OAuth) was implemented and green but its code was lost (see banner above) and must be regenerated; Phases 2–5 remain gated on a Google Cloud OAuth Client ID and the `drive.appdata` sensitive-scope verification.

## Phase progress

| Phase                                                    | Tasks done/total | Status  |
| -------------------------------------------------------- | ---------------- | ------- |
| Phase 1 — Snapshot export/import + tombstones (no OAuth) | 0/8              | pending |
| Phase 2 — CloudSyncPort + Google Drive adapter           | 0/6              | blocked |
| Phase 3 — syncWithCloud merge engine                     | 0/7              | blocked |
| Phase 4 — Hybrid sync UI                                 | 0/5              | blocked |
| Phase 5 — Optional E2E encryption                        | 0/4              | blocked |
| Verification & wrap-up                                   | 0/5              | pending |

> Note: Phase 1 tasks 1.1–1.8 are implemented and ticked in `tasks.md`. The build gate's TS2589 error in `dexie-snapshot-port.ts` and the `no-magic-numbers` warnings were resolved in a follow-up fix — Phase 1 is now **fully green** (build + tests + lint + guards).

Blocked-phase reasons:

- **Phase 2** — Requires a Google Cloud OAuth Client ID (Web) and the `drive.appdata` sensitive-scope verification; external setup, not a code task.
- **Phase 3** — Gated behind Phase 2 OAuth/Drive adapter. The merge engine itself is pure, but the phase's `syncWithCloud` orchestration sits on the OAuth-gated cloud stack.
- **Phase 4** — Connect/disconnect Google account and pull/push wiring require the OAuth Client ID + `drive.appdata` verification from Phase 2.
- **Phase 5** — Encryption is applied to the snapshot before push to Drive; depends on the OAuth-gated cloud sync flow from Phases 2-4.

## Spec coverage

| Capability           | Requirements | Scenarios covered/total |
| -------------------- | ------------ | ----------------------- |
| spa-cloud-sync       | 9            | 3/27                    |
| spa-persistence-port | 2            | 4/4                     |

## Blockers

- Phases 2-5 require a Google Cloud OAuth Client ID (Web) with authorized JS origins for the SPA domain + localhost — external setup, not code.
- The `drive.appdata` scope is classified 'sensitive' and needs Google production verification (free but a review process) before serving more than ~100 users; test-user mode works in the interim.
- Phase 1 has NO external blocker and has been implemented this run.

## Next action

Phase 1 is complete and fully green. Phases 2-5 stay blocked until a Google Cloud OAuth Client ID is provisioned and the `drive.appdata` sensitive-scope verification is in place (test-user mode acceptable interim) — this is external setup, not a code task. Once the OAuth Client ID exists, run the lifecycle workflow again to implement Phase 2 (`CloudSyncPort` + Google Drive adapter).

## This run

Phase 1 (snapshot export/import + tombstones, no OAuth) was implemented in `packages/workout-spa-editor`. All tasks 1.1–1.8 are complete and ticked in `tasks.md`.

**Implemented:**

- New domain types: `Snapshot`, `RemoteSnapshot`, `SnapshotManifest`, `SnapshotTables`, `Tombstone` (`src/types/snapshot.ts`).
- New ports: `TombstoneRepository` (`put`/`list`/`prune`) and `SnapshotPort` (whole-DB dump/restore) so export/import stay pure and never touch `dexie-database`.
- Dexie adapters: `dexie-tombstone-repository.ts`, `dexie-snapshot-port.ts`, plus a `with-tombstones.ts` delete-decorator recording a tombstone in the same transaction.
- Schema: added `CORE_V19` (`tombstones: "[table+id], table, deletedAt"`, with `profileId` stored as a non-indexed plain field so tombstones survive a profile-delete cascade); registered as additive v19 (no upgrade fn).
- Use cases: `export-snapshot.ts` and `import-snapshot.ts` (incl. encrypted `aiProviders` round-trip via real `crypto`).
- In-memory test fakes for the new ports; persistence-port wiring across Dexie + in-memory adapters; v17/v19 migration tests.

**Departure from task wording:** tasks 1.6/1.7 say export/import depend "only on `PersistencePort`". Because per-domain repos lack a uniform `getAll`/`clear`, a focused `SnapshotPort` was introduced instead — use cases still depend only on a port and never import `dexie-database`, satisfying the hexagonal guard and the spec's "pure use case" intent. Flagged for the spec author.

**Quality-gate result:**

- TESTS: PASS — `pnpm --filter @kaiord/workout-spa-editor test` → 537 files / 4335 tests passing, 0 failures.
- BUILD: PASS — TS2589 resolved by narrowing `database.transaction` to an explicit signature (the same `DexieTxScope` pattern used in `dexie-persistence-adapter.ts`); `tsc -b && vite build` now green.
- LINT: PASS — `pnpm lint` exit 0; the 11 `no-magic-numbers` warnings were cleared by hoisting the schema version to a `SCHEMA_VERSION` constant in both snapshot test files.
- MECHANICAL GUARDS: PASS — `pnpm test:scripts` exit 0, including the R-AppDexieImport guard.

Follow-up fix (post-workflow): resolved the TS2589 build error in `dexie-snapshot-port.ts` and the magic-number lint warnings. Phase 1 is now fully green.

## Uncovered scenarios

**spa-cloud-sync** (24 of 27 uncovered):

- CloudSyncPort interface — Application code depends on the port
- CloudSyncPort interface — Pull returns null when no remote snapshot exists
- CloudSyncPort interface — Push returns the new revision identifier
- Google Drive appDataFolder adapter — First push creates the canonical file
- Google Drive appDataFolder adapter — Subsequent push updates the same file
- Google Drive appDataFolder adapter — Pull reads file and revision together
- Google Drive appDataFolder adapter — No backend dependency
- Last-write-wins conflict resolution — Newer local edit wins
- Last-write-wins conflict resolution — Newer remote edit wins
- Last-write-wins conflict resolution — Record present on only one side is kept
- Last-write-wins conflict resolution — Timestampless table merges whole-record
- Deletions propagate via tombstones — Delete on device A removes the record on device B
- Deletions propagate via tombstones — Re-creation newer than the tombstone wins
- Optimistic concurrency on push — Stale revision triggers re-merge
- Optimistic concurrency on push — Clean revision pushes directly
- Hybrid sync triggers — Pull on app open
- Hybrid sync triggers — Debounced push after edits
- Hybrid sync triggers — Manual sync available
- Hybrid sync triggers — Offline degradation
- Optional end-to-end encryption — Encrypted snapshot is unreadable without the passphrase
- Optional end-to-end encryption — Receiving device prompts for the passphrase
- Optional end-to-end encryption — One-time plaintext warning for AI keys
- Account connection UI — Connect an account
- Account connection UI — Disconnect stops sync but keeps data

**spa-cloud-sync** (3 covered): Full-database snapshot — Snapshot includes all tables; Export then import round-trips; AI provider rows survive the round-trip.

**spa-persistence-port** (all 4 covered): Deleting a workout records a tombstone; Tombstone and delete are atomic; Call sites are unchanged (only the single-record `delete(id)` path is asserted — `deleteByProfile` is not wrapped by the decorator and is uncovered); Additive upgrade on existing database.
