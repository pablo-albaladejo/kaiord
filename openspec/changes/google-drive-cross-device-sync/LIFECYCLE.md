# Lifecycle Dashboard — google-drive-cross-device-sync

> Generated by openspec-lifecycle workflow

Artifacts complete: **yes**. Phase 1 (local snapshot export/import + tombstones, no OAuth) and Phase 2 (CloudSyncPort + Google Drive adapter) are both **complete and fully green**. OAuth is now provisioned (Client ID `1059521446940-tplqmpmbrmp8qgsjn4r195gf75un4vt1.apps.googleusercontent.com` via `VITE_GOOGLE_OAUTH_CLIENT_ID`, `drive.appdata` scope confirmed non-sensitive), which unblocked Phase 2. Phases 3–6 remain gated only on their upstream code dependencies (sequential implementation order).

## Phase progress

| Phase                                                    | Tasks done/total | Status   |
| -------------------------------------------------------- | ---------------- | -------- |
| Phase 1 — Snapshot export/import + tombstones (no OAuth) | 8/8              | complete |
| Phase 2 — CloudSyncPort + Google Drive adapter           | 6/6              | complete |
| Phase 3 — syncWithCloud merge engine                     | 0/7              | blocked  |
| Phase 4 — Hybrid sync UI                                 | 0/5              | blocked  |
| Phase 5 — Optional E2E encryption                        | 0/4              | blocked  |
| Verification & wrap-up                                   | 0/5              | blocked  |

### Blocked-phase reasons

- **Phase 3** — Depends on Phase 2 (CloudSyncPort) code. Phase 2 is now complete, so this is the next actionable implementation target.
- **Phase 4** — Depends on Phase 2–3 code (CloudSyncPort + syncWithCloud merge engine), which is not yet complete.
- **Phase 5** — Depends on Phase 2–4 code (sync adapter, merge engine, sync UI), which is not yet complete.
- **Verification & wrap-up** — Final verification depends on Phases 2–5 being complete; only Phases 1–2 are done so far.

## Spec coverage

| Capability           | Requirements | Scenarios covered/total |
| -------------------- | ------------ | ----------------------- |
| spa-cloud-sync       | 10           | 5/29                    |
| spa-persistence-port | 2            | 4/4                     |

## Blockers

- No external blockers reported. OAuth is provisioned (Client ID `1059521446940-tplqmpmbrmp8qgsjn4r195gf75un4vt1.apps.googleusercontent.com` via `VITE_GOOGLE_OAUTH_CLIENT_ID`, `drive.appdata` scope confirmed non-sensitive), so Phase 2 was unblocked and is now complete. Phases 3–6 remain status-blocked only by their upstream code dependencies (sequential implementation order), not by any external/process blocker.

## Next action

Implement Phase 2 (task group 2): define the CloudSyncPort interface with an in-memory fake, wire the Google Cloud OAuth Client ID config surface (now provisioned: `1059521446940-tplqmpmbrmp8qgsjn4r195gf75un4vt1.apps.googleusercontent.com` via `VITE_GOOGLE_OAUTH_CLIENT_ID` with `drive.appdata` scope), and build the `googleDriveCloudSyncAdapter` (GIS auth + Drive REST file I/O against `appDataFolder`), with unit tests against mocked GIS/fetch. OAuth is now provisioned and the `drive.appdata` scope is confirmed non-sensitive, so Phase 2 is unblocked and actionable.

> Update: Per the latest run, Phase 2 has been completed and is fully green. The actionable next target is now **Phase 3** — the `syncWithCloud` merge engine (last-write-wins, tombstone honoring, optimistic-concurrency retry loop).

## This run

Implemented **Phase 2 — CloudSyncPort + Google Drive adapter** (tasks 2.1–2.6) via TDD. All 6 tasks ticked in `tasks.md`.

**Files created:**

- `packages/workout-spa-editor/src/ports/cloud-sync-port.ts` — `CloudSyncPort` interface (`isAuthenticated`, `authenticate`, `pull`, `push`).
- `packages/workout-spa-editor/src/test-utils/in-memory-cloud-sync-port.ts` (+ `.test.ts`) — in-memory fake with optimistic-concurrency enforcement for use-case tests.
- `packages/workout-spa-editor/src/lib/cloud-sync/google-oauth-config.ts` (+ `.test.ts`) — single config surface; `resolveGoogleOAuthClientId()` reads `import.meta.env.VITE_GOOGLE_OAUTH_CLIENT_ID` and throws a clear error if unset; exports `DRIVE_APPDATA_SCOPE`, `GIS_SCRIPT_SRC`.
- `packages/workout-spa-editor/src/lib/cloud-sync/load-gis-script.ts` (+ `.test.ts`) — lazy, idempotent GIS script loader.
- `packages/workout-spa-editor/src/lib/cloud-sync/gis-types.ts` — local minimal GIS type surface (no `@types` dependency).
- `packages/workout-spa-editor/src/adapters/cloud-sync/gis-token-client.ts` (+ `.test.ts`) — `createGisAuth` via `initTokenClient`, silent refresh with `prompt: ""`, mocked-GIS unit tests.
- `packages/workout-spa-editor/src/adapters/cloud-sync/drive-rest.ts`, `drive-rest-endpoints.ts`, `drive-multipart.ts` (+ `drive-rest.test.ts`) — Drive REST I/O over `fetch`: locate-or-create `kaiord-snapshot.json` in `appDataFolder`, multipart create, media PATCH update, `alt=media` download, `fields=headRevisionId`. `pull()`→null when absent, `{snapshot, headRevisionId}` when present.
- `packages/workout-spa-editor/src/adapters/cloud-sync/google-drive-cloud-sync-adapter.ts` (+ `.test.ts`) — composes auth + REST into `CloudSyncPort` (auth + I/O only, zero merge logic); `createGoogleDriveCloudSync()` default factory.
- `packages/workout-spa-editor/src/types/vite-env.d.ts` — typed env augmentation for the new var.
- `packages/workout-spa-editor/.env` — committed public OAuth Client ID default.

**Files changed:**

- `.gitignore` — negation `!packages/workout-spa-editor/.env` so the public (non-secret) client ID can be committed.
- `packages/workout-spa-editor/.env.example` — documented the var, scope, and verification caveat.

**Implementation notes:** No Google SDK dependency was added (verified `package.json`); the adapter imports only its own auth/REST helpers and domain types — merge logic is absent by construction (deferred to Phase 3). The env var read is exercised in tests via `vi.stubEnv`; the real OAuth consent flow and live Drive calls are not exercisable in jsdom and are covered by mocked-GIS/mocked-`fetch` unit tests as the tasks specify.

**Quality-gate result:** **PASS.**

- Tests: **PASS** — 545 test files, 4366 tests passed, 0 failed (+24 new) — 72.25s.
- Build: **PASS** (602ms). One non-fatal advisory only: `[INEFFECTIVE_DYNAMIC_IMPORT]` for `OnboardingTutorial.tsx` (dynamically imported by `AppTutorial.tsx` but also statically imported by `HelpSection.tsx`/`use-onboarding-tutorial.ts`). Build exited 0.
- Lint (`pnpm lint`): **PASS** (exit 0) — tsc/eslint/prettier clean across all packages; archive-index, archive-followups, privacy-policy, and cspell all passed.
- Mechanical guards (`pnpm test:scripts`): **PASS** (exit 0) — 485 tests, 71 suites, 0 failures (zustand-writethrough, PII-leakage, library-dual-mount, session-match-id-shape, SPA-fallback guards). Test-convention guards (`should ` title + AAA) pass on all new test files.

## Uncovered scenarios

The following spec scenarios have no executable test coverage yet (covered=false):

### spa-cloud-sync (24 of 29 uncovered)

- Google Drive appDataFolder adapter — No backend dependency — Architectural/deployment assertion (functions from static hosting, no server-side component); no executable test.
- Last-write-wins conflict resolution — Newer local edit wins — No merge use case exists (`syncWithCloud`/`mergeSnapshots` unimplemented).
- Last-write-wins conflict resolution — Newer remote edit wins — No per-record `updatedAt` comparison logic exists.
- Last-write-wins conflict resolution — Record present on only one side is kept — Union-of-records behavior unimplemented/untested.
- Last-write-wins conflict resolution — Timestampless table merges whole-record — Whole-record meta/usage merge keyed on manifest `exportedAt` does not exist.
- Deletions propagate via tombstones — Delete on device A removes the record on device B — Tombstone storage/round-trip exists, but the merge that honors tombstones is not implemented.
- Deletions propagate via tombstones — Re-creation newer than the tombstone wins — No tombstone-vs-record timestamp resolution in any merge function.
- Optimistic concurrency on push — Stale revision triggers re-merge — No `syncWithCloud` re-pull/re-merge/retry orchestration exists.
- Optimistic concurrency on push — Clean revision pushes directly — No use case records the returned revision at the orchestration level.
- Hybrid sync triggers — Pull on app open — No pull-on-boot trigger wired to persistence boot.
- Hybrid sync triggers — Debounced push after edits — No debounced auto-push implemented.
- Hybrid sync triggers — Manual sync available — No "Sync now" control exists.
- Hybrid sync triggers — Offline degradation — No non-fatal sync-failure handling at the orchestration level.
- Optional end-to-end encryption — Encrypted snapshot is unreadable without the passphrase — No snapshot-level encryption path; `manifest.encrypted` always false.
- Optional end-to-end encryption — Receiving device prompts for the passphrase — No passphrase-prompt-on-pull UI or gating logic.
- Optional end-to-end encryption — One-time plaintext warning for AI keys — No one-time cleartext-warning UI exists.
- Account connection UI — Connect an account — No Settings connect-Google-account control; GIS token client exists at lib/adapter layer only.
- Account connection UI — Disconnect stops sync but keeps data — No disconnect control and no sync-trigger machinery to stop.

> Covered (5 of 29): CloudSyncPort interface — Application code depends on the port; CloudSyncPort interface — Pull returns null when no remote snapshot exists; CloudSyncPort interface — Push returns the new revision identifier; Google Drive appDataFolder adapter — First push creates the canonical file; Google Drive appDataFolder adapter — Subsequent push updates the same file; Google Drive appDataFolder adapter — Pull reads file and revision together. (Plus the prior Phase 1 SnapshotPort/full-database scenarios.)

### spa-persistence-port (0 of 4 uncovered)

All 4 scenarios are covered: Deleting a workout records a tombstone; Tombstone and delete are atomic; Call sites are unchanged; Additive upgrade on existing database.
