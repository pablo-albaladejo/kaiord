I have a complete picture of all 31 test files and their implementations. Here is the audit.

---

## packages/core test-suite audit — subtle quality dimensions

### HIGH severity

- [SEVERITY high] [CATEGORY redundant] `src/adapters/logger/console-logger.test.ts:31-129` — 8 `it()` blocks ("should call console.debug with message and context", "...with message only", repeated for info/warn/error) each assert `console.X` was called with the forwarded args — why: the impl is a 4-line pure pass-through (`debug: (m,c) => console.debug(m,c)`); these tests assert a mock was called (wiring, not behavior) and re-prove the same delegation class 8×. The "with message only" variant adds nothing because `console.X(m, undefined)` is just the default-arg behavior — suggested fix: collapse to ONE `it.each([["debug"],["info"],["warn"],["error"]])` proving forwarding, drop the "message only" duplicates; delete ~6 tests.

- [SEVERITY high] [CATEGORY redundant] `src/adapters/logger/console-logger.test.ts:17-29` — "should create logger with all required methods" duplicates the contract already implied by every other test in the file (each calls a method) — why: a "has these keys" smoke test on a 4-key object literal is dead weight once any method is exercised — suggested fix: delete.

- [SEVERITY high] [CATEGORY redundant] `src/ports/logger.test.ts:6` — "should define logger interface" constructs a `Logger` literal and asserts its 4 keys are defined — why: `Logger` is a pure `type` (compile-time only); this asserts TypeScript object-literal semantics, not any product logic. It can never fail for a real reason — suggested fix: delete the entire file (a type with no runtime artifact needs no runtime test).

- [SEVERITY high] [CATEGORY redundant] `src/adapters/analytics/noop-analytics.test.ts:6-72` — 6 tests on a 5-line no-op factory: "should create adapter with all required methods", "should not throw when pageView…", "…event with props", "…event without props", "should produce no side effects on pageView", "…on event" — why: a no-op returning `undefined` has exactly one behavior class ("does nothing, returns undefined"); the props/no-props split and the not-throw/undefined split are the same equivalence class twice — suggested fix: keep ONE "should be callable without side effects" test (assert both calls return undefined and don't throw); delete ~5.

- [SEVERITY high] [CATEGORY redundant] `src/test-utils/fixtures.test.ts` (whole file, 340 lines) + `src/test-utils/index.test.ts` (whole file) — massively over-tested test-helper. `fixtures.test.ts` has e.g. `loadKrdFixture` "should throw error for non-existent KRD file" duplicated verbatim at L179-182 AND L184-187 (literal copy-paste). The "should load all available X" loop tests exist in 5 flavors (fit, krd-parsed, krd-raw, pairs, by-constant) all asserting `version==="1.0"` — why: these are test utilities, not product code; the file re-proves `readFileSync`+`JSON.parse` work and that 4 fixture files exist on disk, across ~30 assertions. `index.test.ts` then re-tests all 6 re-exports a third time — why: a re-export barrel needs at most a single "barrel exposes names" check, not a full behavioral re-run — suggested fix: collapse `fixtures.test.ts` to ~6 tests (one per function: happy path + the throw), delete the exact-duplicate at L184-187, delete `index.test.ts` entirely (re-export coverage is implied). ~25 tests deletable.

- [SEVERITY high] [CATEGORY redundant] `src/domain/types/errors.test.ts` (578 lines) — the per-class pattern ("should create error with message", "should create … with cause", "should work with instanceof checks", "should preserve stack trace", "should create … via factory", "should create … with cause via factory") is repeated near-identically for FitParsingError, TcxParsingError, ZwiftParsingError (4-6 tests each) and the violation/errors-array pattern for KrdValidationError/ToleranceExceededError/ZwiftValidationError — why: all \*ParsingError classes share one base shape (message+cause+name+stack+instanceof); proving it once parametrized proves the class factory. The "preserve stack trace" + "work with instanceof" assertions also already live inside the "create error with message" test (which asserts `instanceof` and `stack`) — suggested fix: one `it.each` over `[[FitParsingError,"FitParsingError"],[Tcx…],[Zwift…]]` for the message/cause/instanceof/stack contract; one `it.each` for the errors-array classes; keep the `createX` factory delegation as a single parametrized case. The "Error catching patterns" describe (L485-576) re-proves `instanceof` in a `try/catch` — same equivalence class as the instanceof tests above, delete it. ~20 tests deletable.

- [SEVERITY high] [CATEGORY gap] `src/application/round-trip/compare-laps.ts`, `compare-sessions.ts`, `compare-records.ts`, `check-field.ts`, `compare-entity-fields.ts` — NO co-located test; only exercised through `validate-round-trip.test.ts` where the `ToleranceChecker` is fully mocked to return canned violations — why: because the checker is mocked, the round-trip tests prove the _plumbing_ (field-name prefixing `sessions[0].avgPower`) but NEVER prove the real branch logic in `compareEntityFields`/`checkField`: the `value1 !== undefined && value2 !== undefined` skip-when-absent guard (the one non-trivial branch) is untested, and the `Math.min(laps1.length, laps2.length)` length-mismatch handling is untested. `maxHeartRate` appears in `LAP_CHECKS` but no test drives it — suggested fix: add a small unit test for `checkField` (undefined-skip branch) and `compareLaps` length-mismatch; these are the actual logic, currently covered only by assertion-of-mock-output.

- [SEVERITY high] [CATEGORY redundant] `src/application/round-trip/validate-round-trip.test.ts` — `validateFitToKrdToFit` and `validateKrdToFitToKrd` each have their own "should return empty violations when round-trip succeeds", "should return violations when tolerance exceeded", "should propagate FitReader errors", "should propagate FitWriter errors", and the 3-case `compareKRDs` describe re-drives session/lap/record violation detection with ~120 lines of mock setup each — why: with the tolerance checker mocked, "detect violations in session/lap/record metrics" (L362-636) is testing that `compareSessions/Laps/Records` push the mock's return value with the right field prefix — the same wiring the L85 and L238 "should return violations" tests already prove for one field. The session/lap/record trio is one equivalence class (iterate entity array, prefix field name) tested 3× — suggested fix: keep one violation-detection test that covers all three entity arrays in a single KRD; the error-propagation tests can be `it.each` over reader/writer. ~3-4 tests deletable.

### MEDIUM severity

- [SEVERITY med] [CATEGORY name] `src/application/from-format.test.ts:52`, `to-format.test.ts:51` — "should log when logger is provided" asserts `infoSpy toHaveBeenCalledWith("Converting binary format to KRD")` etc. — why: this is an implementation-coupled assertion on exact internal log strings (testing a mock was called with a magic string, not observable product behavior); a wording change to a log line breaks the test for no functional reason — suggested fix: either drop these (logging is not a contract) or assert only that _some_ info log fired; do not pin literal log copy.

- [SEVERITY med] [CATEGORY name] `src/application/round-trip/validate-round-trip.test.ts:54,207,362` — `describe("validateFitToKrdToFit")`, `describe("validateKrdToFitToKrd")`, `describe("compareKRDs")` name internal methods/functions rather than behavior — why: `compareKRDs` is an internal helper not even called directly in that block (the test calls `validateFitToKrdToFit`); the describe label misleads about what's under test — suggested fix: rename to behavior, e.g. "round-trip validation surfaces metric drift across sessions, laps, and records".

- [SEVERITY med] [CATEGORY gap] `src/domain/validation/validate-krd.ts` — no co-located test — why: `validateKrd` is the central validation boundary (used by from-format/to-format). Its specific behavior — building the joined `"KRD validation failed: field: message, …"` error string and returning `result.data` (the parsed, not the input, object) — is only observed indirectly via `KrdValidationError` rejections; the message-formatting branch and the parse-vs-passthrough return are untested directly — suggested fix: add a focused test asserting the thrown message shape and that returned object is the Zod-parsed result.

- [SEVERITY med] [CATEGORY redundant] `src/domain/validation/schema-validator.test.ts:31-162` — separate tests "missing version", "missing type", "missing metadata", "version invalid format", "type invalid value", "metadata.created not ISO", "metadata.sport missing" each `safeParse` an almost-identical object and assert `errors.some(e=>e.field===X)` — why: this re-tests `krdSchema` (a Zod schema) field-by-field through the thin `createSchemaValidator` wrapper; the wrapper's only logic is `issue.path.join(".") || "root"` mapping. One parametrized case proves the path-mapping; the rest re-prove Zod required/enum/datetime validators — suggested fix: `it.each` over `[[{…},"version"],[{…},"type"],…]`; keep "map Zod error to ValidationError with field path" (L224) as the one test that actually proves the wrapper's path-join logic. ~5 tests collapsible.

- [SEVERITY med] [CATEGORY redundant] `src/domain/validation/tolerance-checker.test.ts` — every metric (`checkTime/Distance/Power/HeartRate/Cadence/Pace`) has its own "within tolerance → null" + "exceeds → violation" pair, all hitting the identical shared `check()` helper (`Math.abs(diff) > tol`) — why: all 6 checkers are the same code path with a different field string/threshold; this is one equivalence class tested 6× plus the `edge cases` describe (zero/large/small) which exercise the same `>` comparison — suggested fix: `it.each` over the 6 checker methods for the boundary trio (under/equal/over); keep `pace` separately only because it uses a fractional tolerance. The "should reject missing fields"/"reject negative" on `toleranceConfigSchema` (L401-432) re-test Zod `.positive()` — keep one. ~6-8 tests collapsible.

- [SEVERITY med] [CATEGORY redundant] health schemas (`weight.test.ts`, `hrv.test.ts`, `daily.test.ts`, `body-composition.test.ts`, `sleep.test.ts`, `stress.test.ts`) — every file independently has a "should reject a wrong major version" test (6 copies) that re-proves the shared `z.string().regex(/^2\.\d+$/)` pattern, and weight+sleep both have "should accept a forward-compatible v2.1 payload" — why: the `2.x` version gate is the same regex across all six schemas; proving it once (or in a shared parametrized helper) suffices. Per-field "reject missing X" / "reject non-positive X" largely re-test Zod's `.positive()`/required — why: only the _custom_ `.superRefine` logic is our code — suggested fix: keep every superRefine test (sleep stage-sum tolerance, stress endTime≥startTime & peak≥average, body-composition at-least-one-field — these ARE product logic and are correctly named), but factor the 6 identical "wrong major version" cases into one shared parametrized test over all health schemas. ~5 tests collapsible.

### LOW severity

- [SEVERITY low] [CATEGORY name] `src/domain/type-guards.test.ts:45` — "should work as a type-narrowing filter" — vague "should work"; the assertion is really "should narrow a mixed array to RepetitionBlocks under Array.filter" — suggested fix: rename to the behavior.

- [SEVERITY low] [CATEGORY redundant] `src/domain/type-guards.test.ts:45-66` — the "type-narrowing filter" test re-exercises the same true/false discrimination already proven by L7 and L28 — why: `.filter(isRepetitionBlock)` returning 1 of 3 is the same guard, just wrapped in Array.filter (a library) — suggested fix: drop or fold into the true/false cases.

- [SEVERITY low] [CATEGORY redundant] `src/test-utils/fixtures.test.ts:316,328` describe `"Path Resolution"` with "should handle both dev and production paths" / "should resolve paths consistently across all functions" — why: both just re-assert `existsSync` + `JSON.parse(raw) === parsed`, already covered by the per-function tests above; names also restate mechanics not behavior — suggested fix: delete the describe.

- [SEVERITY low] [CATEGORY name] `src/domain/converters/length-unit.converter.test.ts:26,38,62` — "should convert yards to meters" / "should convert 50 yards to meters" / "should handle decimal lengths in yards" — why: 50-yards and 27.5-yards are the same equivalence class as the first yards→meters case (single `*0.9144` branch); the names restate literal inputs — suggested fix: one `it.each` of yard values; keep one meters-passthrough. ~2 collapsible.

- [SEVERITY low] [CATEGORY redundant] `src/domain/converters/workout-to-krd.converter.test.ts:105,116` — "should throw KrdValidationError for invalid workout" and "…for missing steps" are the same equivalence class (workoutSchema rejects → throws) — why: both prove the single safeParse-fail branch — suggested fix: one parametrized invalid-input case.

- [SEVERITY low] [CATEGORY redundant] `src/test-utils/index.test.ts` "should export loadKrdFixtureRaw" et al. vs `fixtures.test.ts` — the entire index file re-tests the same 6 functions through the barrel — already counted under the HIGH fixtures finding.

- [SEVERITY low] [CATEGORY name] `src/domain/schemas/krd/index.test.ts:108` — "should validate a health.sleep payload strictly when present" but the assertion only checks `success === false` for a `version:"1.0"` sleep — the name promises "strict validation" generally; it proves one rejected version — suggested fix: rename to "should reject a health payload with a sub-v2 sleep version".

- [SEVERITY low] [CATEGORY gap] `src/protocol/profile-snapshot.ts` — `fingerprintSnapshot` is well covered, but the `try { JSON.stringify } catch` branch in `profileSnapshotSchema` (the "Invalid snapshot payload" on a stringify throw for a non-circular, non-poisoned value) is only reached via the circular-ref test; acceptable. No action needed beyond noting completeness here is actually strong.

---

### Package verdict

| Dimension        | Grade | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ---------------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Redundancy**   | **D** | Heavy duplication in trivial/infra code: console-logger (8→2), noop-analytics (6→1), errors (578 lines, ~20 deletable), test-utils fixtures+index (~25 deletable incl. a literal copy-paste at L184-187), per-metric tolerance-checker, per-schema "wrong major version" ×6. The codebase tests no-ops and re-exports harder than its real logic.                                                                                                                      |
| **Completeness** | **C** | Strong on genuine domain logic (power-zones round-trip, health superRefines, profile-snapshot prototype-pollution/size guards, canonical-hash byte-compat). But the real round-trip comparison logic (`check-field` undefined-skip branch, `compare-laps` length-mismatch, `maxHeartRate` check) is untested because the checker is mocked, and `validate-krd` (central boundary) has no direct test. Over-tested trivial paths sit next to under-tested complex ones. |
| **Naming**       | **B** | Mostly good business-language titles (power-zones, sport-category, health schemas are exemplary). Dragged down by mock-coupled names ("should log when logger is provided" pinning literal log strings), file/method-named describes in round-trip, and a few "should work"/literal-restating titles.                                                                                                                                                                  |

### Top 3 highest-leverage actions

1. **Delete/parametrize the infrastructure test bloat**: console-logger, noop-analytics, ports/logger, test-utils (fixtures + index), and errors. This is ~70 of the deletable tests and removes the suite's worst signal-to-noise — none of it proves product behavior.
2. **Add real unit tests for the round-trip comparison helpers** (`check-field` undefined-skip, `compare-entity-fields`/`compare-laps` length-mismatch and `maxHeartRate`) with a _real_ `createToleranceChecker`, not a mock — this is the one place where deleting redundant tests would otherwise expose a genuine coverage gap.
3. **Factor cross-cutting duplicated cases into `it.each`/shared helpers**: the 6 identical health "wrong major version" tests, the 6 tolerance-checker metric pairs, and the field-by-field schema-validator cases — converting ~20 near-identical blocks into ~4 parametrized tests.

### Approx tests deletable without losing proof of functionality

**~75–85 tests** (concentrated in: console-logger ~6, noop-analytics ~5, ports/logger 1, test-utils fixtures ~20 + index 6, errors ~20, tolerance-checker ~7, schema-validator ~5, health "wrong major version" ~5, round-trip ~3, converters ~3, type-guards 1). Net effect after also _adding_ ~3-4 targeted tests for the real round-trip/validate-krd gaps: a substantially smaller suite that proves strictly more.

===NEXT-BLOCK===

Audit complete. I reviewed all 31 test files in `packages/core` against their implementations, focusing on the subtle quality dimensions (redundancy, minimality vs completeness, semantic naming) rather than the mechanically-enforced conventions.

Key findings delivered above. The headline takeaways:

- **Redundancy (grade D)**: The suite over-tests trivial infrastructure (no-op analytics, console-logger pass-through, error classes, test-utils, the `Logger` type itself) while a literal copy-paste duplicate sits in `fixtures.test.ts:184-187`. ~75-85 tests are deletable.

- **Completeness (grade C)**: The real round-trip comparison logic (`check-field` undefined-skip branch, `compare-laps` length-mismatch, the `maxHeartRate` field) is only "covered" through a fully-mocked `ToleranceChecker`, so the actual branch logic is never exercised. `validate-krd.ts`, the central validation boundary, has no direct test.

- **Naming (grade B)**: Mostly strong business-language titles (power-zones, health schemas, sport-category are exemplary), undercut by mock-coupled assertions on literal log strings and a few file/method-named `describe` blocks.

The three highest-leverage actions and the deletable-test estimate are in the verdict section of my report above.

Relevant implementation files lacking direct/honest coverage:

- /Users/pablo/development/kaiord/packages/core/src/application/round-trip/check-field.ts
- /Users/pablo/development/kaiord/packages/core/src/application/round-trip/compare-entity-fields.ts
- /Users/pablo/development/kaiord/packages/core/src/application/round-trip/compare-laps.ts
- /Users/pablo/development/kaiord/packages/core/src/application/round-trip/compare-sessions.ts
- /Users/pablo/development/kaiord/packages/core/src/application/round-trip/compare-records.ts
- /Users/pablo/development/kaiord/packages/core/src/domain/validation/validate-krd.ts

===NEXT-BLOCK===

## packages/core test-suite audit — subtle quality dimensions

### HIGH severity

- [high][redundant] `src/adapters/logger/console-logger.test.ts:31-129` — 8 it() blocks ("should call console.debug with message and context", "...with message only", repeated for info/warn/error) each assert `console.X` was called with forwarded args — impl is a 4-line pure pass-through (`debug: (m,c) => console.debug(m,c)`); these assert a mock was called (wiring, not behavior) and re-prove the same delegation class 8×; the "message only" variant only proves default-arg behavior — fix: one `it.each([["debug"],["info"],["warn"],["error"]])`; delete ~6 tests.
- [high][redundant] `src/adapters/logger/console-logger.test.ts:17-29` — "should create logger with all required methods" — a "has these keys" smoke test on a 4-key object literal, redundant once any method is exercised — fix: delete.
- [high][redundant] `src/ports/logger.test.ts:6` — "should define logger interface" constructs a `Logger` literal and asserts 4 keys defined — `Logger` is a pure compile-time `type`; tests TS object-literal semantics, never fails for a real reason — fix: delete the file.
- [high][redundant] `src/adapters/analytics/noop-analytics.test.ts:6-72` — 6 tests on a 5-line no-op factory ("create adapter with all required methods", "not throw…pageView", "…event with props", "…event without props", "no side effects on pageView", "…on event") — one equivalence class ("does nothing, returns undefined") tested 6× — fix: keep one "should be callable without side effects"; delete ~5.
- [high][redundant] `src/test-utils/fixtures.test.ts` (340 lines) + `src/test-utils/index.test.ts` — over-tested helper; `loadKrdFixture` "should throw error for non-existent KRD file" is duplicated verbatim at L179-182 AND L184-187 (literal copy-paste); "should load all available X" loops exist in 5 flavors all asserting `version==="1.0"`; `index.test.ts` re-tests all 6 re-exports a third time — re-proves `readFileSync`+`JSON.parse` and that 4 files exist on disk — fix: collapse `fixtures.test.ts` to ~6 tests (one per fn: happy + throw), delete L184-187 dup, delete `index.test.ts`; ~25 deletable.
- [high][redundant] `src/domain/types/errors.test.ts` (578 lines) — per-class pattern ("create with message", "…with cause", "work with instanceof", "preserve stack trace", "…via factory", "…with cause via factory") repeated near-identically for Fit/Tcx/Zwift ParsingError, plus errors-array pattern for Krd/Tolerance/ZwiftValidation; "preserve stack"/"instanceof" assertions already live inside "create with message"; "Error catching patterns" (L485-576) re-proves instanceof in try/catch — fix: one `it.each` over the parsing classes, one over the errors-array classes, factory as a single parametrized case, delete catching-patterns block; ~20 deletable.
- [high][gap] `src/application/round-trip/compare-laps.ts`, `compare-sessions.ts`, `compare-records.ts`, `check-field.ts`, `compare-entity-fields.ts` — NO co-located test; only exercised via `validate-round-trip.test.ts` with a fully-mocked `ToleranceChecker` returning canned violations — so the real branch logic is untested: `check-field`'s `value1 !== undefined && value2 !== undefined` skip-when-absent guard, `compare-laps`'s `Math.min(len1,len2)` length-mismatch handling, and the `maxHeartRate` entry in `LAP_CHECKS` are never driven — fix: add focused unit tests for `checkField` (undefined-skip) and `compareLaps` (length mismatch + maxHeartRate) with a real checker.
- [high][redundant] `src/application/round-trip/validate-round-trip.test.ts:362-636` — `compareKRDs` describe re-drives session/lap/record violation detection (~120 lines mock setup each) — with the checker mocked, this tests that compare\* push the mock's return with the right field prefix (`sessions[0].avgPower`), the same wiring the L85/L238 "should return violations" tests already prove; session/lap/record is one equivalence class (iterate array, prefix field) tested 3× — fix: one violation-detection test covering all three arrays in a single KRD; error-propagation as `it.each` over reader/writer; ~3-4 deletable.

### MEDIUM severity

- [med][name] `src/application/from-format.test.ts:52`, `to-format.test.ts:51` — "should log when logger is provided" asserts `infoSpy.toHaveBeenCalledWith("Converting binary format to KRD")` — implementation-coupled assertion on exact internal log strings; a wording change breaks the test for no functional reason — fix: drop, or assert only that some info log fired.
- [med][name] `src/application/round-trip/validate-round-trip.test.ts:54,207,362` — `describe("validateFitToKrdToFit")` / `"validateKrdToFitToKrd")` / `"compareKRDs")` name internal methods/functions, not behavior; the compareKRDs block doesn't even call compareKRDs directly — fix: rename to behavior, e.g. "round-trip validation surfaces metric drift across sessions, laps, and records".
- [med][gap] `src/domain/validation/validate-krd.ts` — no co-located test; the central validation boundary's specific behavior (joined `"KRD validation failed: field: message, …"` string; returning `result.data` parsed object, not the input) is only observed indirectly via rejections — fix: add a test asserting the thrown message shape and parsed-return.
- [med][redundant] `src/domain/validation/schema-validator.test.ts:31-162` — separate tests "missing version/type/metadata", "version invalid format", "type invalid value", "metadata.created not ISO", "metadata.sport missing" each safeParse a near-identical object — re-tests `krdSchema` (Zod) field-by-field through a thin wrapper whose only logic is `issue.path.join(".") || "root"` — fix: `it.each`; keep "map Zod error to ValidationError with field path" (L224) which alone proves the wrapper's path-join; ~5 collapsible.
- [med][redundant] `src/domain/validation/tolerance-checker.test.ts` — each of 6 metrics has its own under/over pair hitting the identical shared `check()` helper (`Math.abs(diff) > tol`); "edge cases" (zero/large/small) re-exercise the same `>`; "reject missing fields"/"reject negative" re-test Zod `.positive()` — fix: `it.each` over the 6 methods for under/equal/over; keep pace separate (fractional tol); keep one schema-rejection; ~6-8 collapsible.
- [med][redundant] health schemas (`weight/hrv/daily/body-composition/sleep/stress.test.ts`) — each file has its own "should reject a wrong major version" (6 copies of the shared `regex(/^2\.\d+$/)` gate); weight+sleep both have "accept forward-compatible v2.1"; per-field "reject missing/non-positive" largely re-test Zod — fix: keep every superRefine test (sleep stage-sum tolerance, stress endTime≥startTime & peak≥average, body-composition at-least-one-field — real product logic, well named) but factor the 6 "wrong major version" cases into one shared parametrized test; ~5 collapsible.

### LOW severity

- [low][name] `src/domain/type-guards.test.ts:45` — "should work as a type-narrowing filter" — vague "should work"; behavior is "narrow a mixed array to RepetitionBlocks under Array.filter" — fix: rename.
- [low][redundant] `src/domain/type-guards.test.ts:45-66` — the filter test re-exercises the true/false discrimination already proven at L7/L28, wrapped in Array.filter (a library) — fix: drop or fold in.
- [low][redundant] `src/test-utils/fixtures.test.ts:316,328` — describe "Path Resolution" ("should handle both dev and production paths" / "…consistently across all functions") just re-asserts `existsSync` + `JSON.parse(raw)===parsed`, covered by per-function tests; names restate mechanics — fix: delete the describe.
- [low][name] `src/domain/converters/length-unit.converter.test.ts:26,38,62` — "should convert yards to meters" / "…50 yards…" / "…decimal lengths in yards" — same single `*0.9144` branch; names restate literal inputs — fix: one `it.each` of yard values; ~2 collapsible.
- [low][redundant] `src/domain/converters/workout-to-krd.converter.test.ts:105,116` — "throw KrdValidationError for invalid workout" and "…for missing steps" are the same safeParse-fail branch — fix: one parametrized invalid-input case.
- [low][name] `src/domain/schemas/krd/index.test.ts:108` — "should validate a health.sleep payload strictly when present" but only checks `success===false` for a `version:"1.0"` sleep — fix: rename to "should reject a health payload with a sub-v2 sleep version".
- [low][note] `src/protocol/profile-snapshot.test.ts` — strong coverage (prototype-pollution, circular-ref, 8192-size guard, fingerprint determinism/ignores-generatedAt/profileId-collision); no action — flagged only to record that completeness here is exemplary.

---

### Package verdict

| Dimension        | Grade | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Redundancy**   | **D** | Heavy duplication in trivial/infra code: console-logger (8→2), noop-analytics (6→1), ports/logger (delete), errors (~20 deletable), test-utils fixtures+index (~25 deletable incl. a literal copy-paste at L184-187), per-metric tolerance-checker, per-schema "wrong major version" ×6. The suite tests no-ops and re-exports harder than its real logic.                                                                       |
| **Completeness** | **C** | Strong on genuine domain logic (power-zones round-trip, health superRefines, profile-snapshot guards, canonical-hash byte-compat). But the real round-trip comparison logic (`check-field` undefined-skip, `compare-laps` length-mismatch, `maxHeartRate`) is untested because the checker is mocked, and `validate-krd` (central boundary) has no direct test. Over-tested trivial paths sit next to under-tested complex ones. |
| **Naming**       | **B** | Mostly good business-language titles (power-zones, sport-category, health schemas are exemplary). Dragged down by mock-coupled names pinning literal log strings, file/method-named describes in round-trip, and a few "should work"/literal-restating titles.                                                                                                                                                                   |

### Top 3 highest-leverage actions

1. **Delete/parametrize the infrastructure test bloat** — console-logger, noop-analytics, ports/logger, test-utils (fixtures + index), errors. ~70 of the deletable tests; removes the suite's worst signal-to-noise, none of which proves product behavior.
2. **Add real unit tests for the round-trip comparison helpers** — `check-field` undefined-skip branch, `compare-entity-fields`/`compare-laps` length-mismatch and `maxHeartRate`, using a _real_ `createToleranceChecker` not a mock. This is the one place where deleting redundant tests would otherwise expose a genuine coverage gap.
3. **Factor cross-cutting duplicated cases into `it.each`/shared helpers** — the 6 identical health "wrong major version" tests, the 6 tolerance-checker metric pairs, and the field-by-field schema-validator cases (~20 near-identical blocks → ~4 parametrized tests).

### Approx tests deletable without losing proof of functionality

**~75–85 tests** (console-logger ~6, noop-analytics ~5, ports/logger 1, test-utils fixtures ~20 + index 6, errors ~20, tolerance-checker ~7, schema-validator ~5, health "wrong major version" ~5, round-trip ~3-4, converters ~3, type-guards 1). Net of ~3-4 _added_ targeted tests for the round-trip/`validate-krd` gaps, the result is a substantially smaller suite that proves strictly more.
