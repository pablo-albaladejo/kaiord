# Umami website id (public, not a secret).
# Leave empty in local dev — analytics will be disabled (noop adapter).
# Set in CI/CD to enable Umami analytics in production builds.
VITE_UMAMI_WEBSITE_ID=
