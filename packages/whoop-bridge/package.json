{
  "name": "@kaiord/whoop-bridge",
  "version": "10.0.0",
  "private": true,
  "description": "Chrome extension that bridges the Kaiord SPA to the internal WHOOP API via the user's own app.whoop.com session (no OAuth, no credentials)",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@kaiord/core": "workspace:*",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
