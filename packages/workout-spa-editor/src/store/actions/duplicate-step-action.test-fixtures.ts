/**
 * Co-located numeric fixtures for duplicate-step-action.test.ts.
 *
 * Values are sourced verbatim from the sibling test file so that
 * extracting them silences `no-magic-numbers` without changing
 * test semantics.
 */

export const DUPLICATED_LENGTH_TWO_PLUS_ONE = 3 as const;
export const DUPLICATED_LENGTH_THREE_PLUS_ONE = 4 as const;
export const FOURTH_STEP_INDEX_VALUE = 3 as const;
export const NON_EXISTENT_STEP_INDEX = 5 as const;
