/**
 * Co-located numeric fixtures for reorder-steps-in-block-action.test.ts.
 *
 * Values are sourced verbatim from the sibling test file so that
 * extracting them silences `no-magic-numbers` without changing
 * test semantics.
 */

export const REORDERED_BLOCK_STEP_COUNT = 3 as const;
export const SECONDS_LONG_INTERVAL = 600 as const;
export const SECONDS_SHORT_INTERVAL = 300 as const;
export const OUT_OF_BOUNDS_OVER_INDEX = 5 as const;
