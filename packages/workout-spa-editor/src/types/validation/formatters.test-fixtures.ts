/**
 * Co-located numeric fixtures for formatters.test.ts.
 *
 * Co-located fixture module — pure constants only.
 */

// Mixed-type array used to trigger Zod array-error formatting.
export const MIXED_ARRAY_FIXTURE = [1, "invalid", 3] as const;
