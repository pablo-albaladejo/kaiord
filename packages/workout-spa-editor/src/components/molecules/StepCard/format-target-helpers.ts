/**
 * Format target helpers - Re-exports for modular structure
 */
export { formatCadenceTarget, formatPaceTarget } from "./format-cadence-pace";
export { formatHeartRateTarget, formatPowerTarget } from "./format-power-hr";
