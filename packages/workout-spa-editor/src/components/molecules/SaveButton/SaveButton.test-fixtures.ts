/**
 * Co-located numeric fixtures for SaveButton.test.tsx.
 *
 * Co-located fixture module — pure constants only.
 */

// Three-byte placeholder buffer used for export-workout mocks.
export const MOCK_BUFFER_BYTES_3 = [1, 2, 3] as const;
