/**
 * Co-located numeric fixtures for CoachingSyncButton.test.tsx.
 *
 * Co-located fixture module — pure constants only. Lives outside the
 * `*.test.ts` glob so it is not subject to the test-file
 * `no-magic-numbers` warning.
 */

// Five-minutes-ago offset in milliseconds (5 * 60 * 1000).
export const FIVE_MINUTES_MS = 300_000;
