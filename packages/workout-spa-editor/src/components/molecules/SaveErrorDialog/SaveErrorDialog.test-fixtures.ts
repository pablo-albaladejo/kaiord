/**
 * Co-located numeric fixtures for SaveErrorDialog.test.tsx.
 *
 * Co-located fixture module — pure constants only.
 */

// Number of buttons rendered in the error dialog: header close (X),
// footer Close, and Fix and Retry.
export const ERROR_DIALOG_BUTTON_COUNT = 3;
