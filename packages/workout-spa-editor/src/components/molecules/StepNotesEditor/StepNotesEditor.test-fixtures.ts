/**
 * Co-located numeric fixtures for StepNotesEditor.test.tsx.
 *
 * Co-located fixture module — pure constants only.
 */

// Editor character limit (max length).
export const NOTES_CHARACTER_LIMIT = 256;

// Length used to overflow the character limit (4 over).
export const NOTES_OVER_LIMIT_LENGTH = 260;
