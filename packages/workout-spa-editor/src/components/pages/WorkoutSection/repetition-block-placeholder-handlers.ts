export const createPlaceholderHandlers = () => ({
  handleUngroup: (blockIndex: number) => {
    console.warn(`Ungroup block ${blockIndex} - not yet implemented (task 5)`);
  },
  handleDelete: (blockIndex: number) => {
    console.warn(`Delete block ${blockIndex} - not yet implemented (task 5)`);
  },
});
