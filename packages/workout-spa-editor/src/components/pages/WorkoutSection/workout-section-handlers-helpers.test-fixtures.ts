/**
 * Co-located numeric fixtures for workout-section-handlers-helpers.test.ts.
 *
 * Co-located fixture module — pure constants only.
 */

// Step index used by the second nested step in Block B (intentionally
// distinct from the homonymous main-list index, to assert ItemId
// routing).
export const BLOCK_B_INNER_1_STEP_INDEX = 3;
