/**
 * Co-located numeric fixtures for ManualCreateSection.analytics.test.tsx.
 *
 * Co-located fixture module — pure constants only.
 */

// Drain timer awaited in afterEach to flush pending async work.
export const DRAIN_DELAY_MS = 50;

// Two-byte placeholder buffer used to mock a binary FIT file payload.
export const FIT_PLACEHOLDER_BYTES = [14, 16] as const;
