/**
 * Co-located numeric fixtures for use-coaching-auto-sync.test.tsx.
 *
 * Values are sourced verbatim from the sibling test file so that
 * extracting them silences `no-magic-numbers` without changing
 * test semantics.
 */

export const NO_FIRE_SETTLE_MS = 5 as const;
export const ONE_MINUTE_MS = 60_000 as const;
