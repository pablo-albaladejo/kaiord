/**
 * Co-located numeric fixtures for dexie-usage-migration.test.ts.
 *
 * Values are sourced verbatim from the sibling test file so that
 * extracting them silences `no-magic-numbers` without changing
 * test semantics.
 */

export const LEGACY_USAGE_SMALL_TOKEN_TOTAL = 80 as const;
export const LEGACY_USAGE_LARGE_TOKEN_TOTAL = 750 as const;
