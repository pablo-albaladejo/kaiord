{
  "name": "@kaiord/tanita-bridge",
  "version": "10.0.0",
  "private": true,
  "description": "Chrome extension that reads your MyTANITA body-composition CSV export through your own logged-in mytanita.eu session (no password) and hands it to the Kaiord editor",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@kaiord/core": "workspace:*",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.10"
  }
}
